# Design: Remove orphaned InvoiceClassification statistics dead code

## Skipped

The architect's review (`arch-review.r1.md`) set `Skip Design: true` — this is a
pure backend dead-code removal (repository method, domain types, DTOs, an
AutoMapper registration, and one unused frontend query-key constant). There are
no new or changed UI components, screens, layouts, or visual design decisions.
No design phase output is required.
