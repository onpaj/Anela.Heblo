All tasks complete. Here is the output summary:

---

# Implementation: Expose Shipping Address Fields in GetPackingOrder Response

## What was implemented

Surfaced three already-computed shipping address fields (`ShippingStreet`, `ShippingCity`, `ShippingZip`) from the `PackingOrder` domain object through the `GET /api/shoptet-orders/{code}/packing` endpoint. The adapter already populated these fields; the only gap was the DTO and handler mapping. Null is preserved end-to-end for orders without a delivery address.

## Files created/modified

- `backend/src/Anela.Heblo.Application/Features/ShoptetOrders/UseCases/GetPackingOrder/GetPackingOrderResponse.cs` — added 3 nullable string properties
- `backend/src/Anela.Heblo.Application/Features/ShoptetOrders/UseCases/GetPackingOrder/GetPackingOrderHandler.cs` — added 3 mapping assignments in the object initializer
- `backend/test/Anela.Heblo.Tests/Application/ShoptetOrders/GetPackingOrderHandlerTests.cs` — extended positive-path test with non-null assertions; extended null-path test with `BeNull()` assertions
- `frontend/src/api/generated/api-client.ts` — regenerated by NSwag; gained `shippingStreet?`, `shippingCity?`, `shippingZip?` with full from/toJSON serialization

## Tests

`GetPackingOrderHandlerTests` — all 5 tests pass:
- `Handle_OrderFound_ReturnsMappedResponse` — now asserts all 3 shipping values are correctly mapped
- `Handle_WhenOrderIsInPackingState_ReturnsEligibleWithNullWarning` — now asserts all 3 shipping fields are `null` when not set
- 3 existing negative/error-path tests unchanged and still passing

## How to verify

```bash
dotnet test --filter "FullyQualifiedName~GetPackingOrderHandlerTests"
# Expected: Passed: 5, Failed: 0
```

## Notes

No deviations from the spec. TDD cycle was followed: test written in RED state, properties added to DTO, then handler mapping added to reach GREEN. No new files, no new abstractions, no changes outside the targeted slice.

## PR Summary

Plumbing fix: surface the three shipping address fields (`ShippingStreet`, `ShippingCity`, `ShippingZip`) that the Shoptet adapter already normalizes and stores on `PackingOrder`, but which were missing from `GetPackingOrderResponse` and its handler mapping. The `GET /api/shoptet-orders/{code}/packing` endpoint now returns the delivery address alongside all existing fields.

Null semantics are preserved end-to-end — when the adapter cannot derive an address (personal pickup, missing Shoptet data), the response carries `null`, matching the existing `CustomerNote`/`EshopNote` pattern. The generated TypeScript client picks up the three new optional fields automatically.

### Changes
- `GetPackingOrderResponse.cs` — added `string? ShippingStreet/City/Zip { get; set; }`
- `GetPackingOrderHandler.cs` — added 3 plain assignments in the existing object initializer
- `GetPackingOrderHandlerTests.cs` — extended two existing tests (positive non-null values + null-path assertions)
- `frontend/src/api/generated/api-client.ts` — regenerated with new optional fields

## Status
DONE