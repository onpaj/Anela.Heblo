telemetry-signal: req-400:POST /mcp

**Window:** P7D (2026-08-26 – 2026-09-02)
**Count:** 165 `POST /mcp` requests, **59 (35.8%) returned 400 Bad Request** — steady every day, not a one-off spike:

| Day (UTC) | Total | 400 | 200 | other |
|---|---|---|---|---|
| 08-27 | 16 | 6 | 9 | 1 |
| 08-28 | 26 | 10 | 14 | 2 |
| 08-29 | 26 | 8 | 15 | 3 |
| 08-30 | 27 | 9 | 16 | 2 |
| 08-31 | 23 | 9 | 12 | 2 |
| 09-01 | 28 | 9 | 19 | 0 |
| 09-02 | 19 | 8 | 10 | 1 |

Failure rate is flat at ~30–38% across all 7 days — a chronic baseline, not a regression tied to a
recent deploy. Requests are fast (2–23ms), consistent with the MCP SDK's own request validation
rejecting the request before any handler/tool code runs (a malformed/missing session, wrong
`Mcp-Session-Id`, or an unsupported request shape), not an application exception — `exceptions` and
`traces` have nothing correlated by `operation_Id` for any of these requests.

## Why this has gone unnoticed

`backend/src/Anela.Heblo.API/MCP/McpBadRequestMiddleware.cs` already added diagnostics for exactly
this symptom on **`GET /mcp`** (issue #593: log `User-Agent`/`Accept`/`Origin` when a well-formed
request still 400s, and short-circuit headerless probes to 404 instead of 400 so scanners don't
pollute the signal). That fix only instruments the `GET` path
(`IsMcpGetRequest` filters on `HttpMethods.Get`) — `POST /mcp`, which is where the actual MCP
JSON-RPC traffic (tool calls, session init) flows, has no equivalent logging. So a third of all POST
traffic to the MCP endpoint has been failing, silently, since before this week's window at least,
with no caller identity or failure reason ever recorded.

## Next step

Extend `McpBadRequestMiddleware` (or add a sibling check) to also log `User-Agent`/`Origin`/
`Mcp-Session-Id` presence for `POST /mcp` requests that come back 400, mirroring what already exists
for `GET`. That turns this from "35% of a real endpoint silently fails" into an identifiable root
cause (stale session ID from a reconnecting client vs. a genuinely malformed request) without
guessing further from telemetry alone.

---
_Generated by [Claude Code](https://claude.ai/code)_