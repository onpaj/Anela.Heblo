Plan saved to `artifacts/feat-arch-review-bank-three-import-job-classe/plan.r1.md`.

**Summary:** A 10-task TDD plan that extracts `BankImportJobBase` (abstract template) + `BankImportJobParameters` (internal record) + `BankAccountNames` (constants), reduces the three concrete jobs to ≤30 lines each, and adds the missing test coverage. The plan incorporates all seven architecture amendments — `ILoggerFactory` (preserves per-subclass log category), `sealed` subclasses, `internal sealed record` parameters, `BankAccountNames` at feature root, discovery test, and per-job tests that assert constant identity *and* literal value to guard the wire contract with `BankAccountSettings.Accounts[].Name`. Pre-flight verifies a clean baseline; the final sweep greps for residual magic strings, runs `dotnet build`/`dotnet format`/full test suite, and walks each spec FR.