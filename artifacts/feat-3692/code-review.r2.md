## Review Result: CLEAN

### Blocking (correctness)
- None

### Advisory (cleanup)
- `backend/src/Anela.Heblo.Application/Features/Photobank/Infrastructure/Jobs/PhotobankIndexJob.cs:88-93,105-110,114-119` — the "flush pending batch, increment `upserted`, clear" sequence is still duplicated three times (on delete, on reaching `BatchSize`, and at end-of-loop). This was flagged in round 1 and was not addressed by the fix commit (the fix only touched `UpsertPhotoBatchAsync`'s Phase B). Extracting a small local `async Task FlushPendingBatchAsync()` helper would remove the repetition. Non-blocking.
- `backend/src/Anela.Heblo.Application/Features/Photobank/Infrastructure/Jobs/PhotobankIndexJob.cs:185-214` — `itemMatches` is built as a full `List<(Item, TagNames)>` and then immediately walked again to build `tagNamesByPhoto` keyed by the shared `Photo` instance. Since `itemMatches` is not used for anything else, the dedup-by-photo could be folded into the same loop that computes `allMatchingTagNames`, avoiding one intermediate list. Purely a simplification, no behavioral difference.

### Verification notes
- Confirmed the round-1 gap is closed. `UpsertPhotoBatchAsync` (PhotobankIndexJob.cs:137-238) now builds `tagNamesByPhoto` (a `Dictionary<Photo, IReadOnlyList<string>>`, line 209) between tag-ID resolution (`GetOrCreateTagsAsync`, line 196-198) and the tag-application loop (line 216-235). Because `itemMatches` is iterated in original delta order and `tagNamesByPhoto[photo] = tagNames` is a plain indexer assignment (line 213), a second occurrence of the same `Photo` in `itemMatches` overwrites the first entry — last-item-wins, matching the described semantics. The tag-application loop (line 216) then iterates `tagNamesByPhoto`, so `GetPhotoTagsByPhotoAndSourceAsync`/`RemovePhotoTagsAsync`/`PhotoTagExistsAsync`/`AddPhotoTagAsync` each run at most once per distinct `Photo` per batch, regardless of how many raw delta items shared that `SharePointFileId`. This directly eliminates both round-1 failure modes: the stale-tag-survives case (first occurrence's tag no longer gets a separate, un-deduped removal/add pass) and the duplicate-PK-crash case (no second `AddPhotoTagAsync` call for the same `(PhotoId, TagId)` pair from a second raw item).
- Confirmed the reference-equality assumption is safe: `Photo` (`backend/src/Anela.Heblo.Domain/Features/Photobank/Photo.cs`) is a plain mutable class with no `Equals`/`GetHashCode` override, so `Dictionary<Photo, ...>` uses default reference equality — exactly what the fix relies on, since `photosByFileId` (Phase A, line 143) guarantees both occurrences of a duplicate `SharePointFileId` share the same `Photo` object reference.
- Confirmed `tagIdsByName` resolution via the pre-existing bulk `GetOrCreateTagsAsync` (already used by `ReapplyRulesHandler`, declared in `IPhotobankRepository.cs:39`) requires no interface changes and correctly returns `IReadOnlyDictionary<string,int>`; `TryGetValue` lookups in the tag-application loop are a safe no-op guard if a resolved name is somehow absent.
- Confirmed the two new regression tests exercise both round-1 failure modes directly against the fixed code, not just indirectly via Phase A's dedupe:
  - `UpsertPhotoBatch_DuplicateSharePointFileIdMatchingDifferentTagRules_OnlyLastItemsTagsSurvive` (PhotobankIndexJobTests.cs:602) — two occurrences of one `SharePointFileId` match different tag rules (`tag-a` vs `tag-b`); asserts `GetPhotoTagsByPhotoAndSourceAsync`/`RemovePhotoTagsAsync`/`AddPhotoTagAsync` each fire exactly once and only `tag-b` (the last item's match) is applied — this is precisely the "stale-tag-survives" scenario from the round-1 review.
  - `UpsertPhotoBatch_DuplicateSharePointFileIdMatchingSameTagRule_AppliesTagOnceWithNoDuplicateKeyRisk` (PhotobankIndexJobTests.cs:722) — both occurrences match the same rule/tag, with `PhotoTagExistsAsync` stubbed to always return `false` (simulating real-DB inability to see in-batch unflushed inserts); asserts `AddPhotoTagAsync` is called exactly once for that `(PhotoId, TagId)` — this is precisely the "duplicate-key-crash-risk" scenario.
  - Both tests would fail against the pre-fix code (which iterated `itemMatches`/raw batch items directly instead of the deduped `tagNamesByPhoto`), so they are meaningful regression coverage, not just tautological re-assertions of the new code path.
- Independently rebuilt the solution (`dotnet build Anela.Heblo.sln`) — 0 errors (251 pre-existing warnings, none touching the changed files).
- Ran `dotnet test ... --filter "FullyQualifiedName~PhotobankIndexJobTests"` — 11/11 passed.
- Ran the broader `--filter "FullyQualifiedName~Photobank"` suite — 192 passed, 3 failed, but all 3 failures are `PhotobankRepositoryGetTagsSqlShapeTests` cases that fail with `Docker is either not running or misconfigured` (a Testcontainers/Postgres fixture requiring a Docker daemon unavailable in this sandbox) — an environment limitation unrelated to this change, not a code defect.
- Fresh pass over the rest of the diff (both files in full, 829 lines total — `PhotobankIndexJob.cs` and `PhotobankIndexJobTests.cs` are the only two files touched): the batch-flush-before-delete logic (lines 84-93), end-of-loop flush (114-119), and root bookkeeping/error handling (121-134) are unchanged from the original (already-reviewed) implementation and correctly preserve FR-3/FR-4 semantics (root `DeltaLink`/`LastIndexedAt` written once after the full loop, exceptions still caught and logged per root, `upserted`/`deleted` counters accurate). No new correctness issues found.
