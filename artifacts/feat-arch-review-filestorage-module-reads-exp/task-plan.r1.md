Plan saved. Per the pipeline instructions, skipping the interactive execution-handoff prompt — the file itself is the artifact.

**Summary:** Wrote `docs/superpowers/plans/2026-06-12-decouple-filestorage-from-expeditionlist-config.md`. The plan covers 5 prerequisites (Key Vault provisioning + Test-env confirmation) and 11 TDD tasks: add `FileStorageOptions`, rewrite the failing module tests, replace the module body with env-aware options binding + fail-fast `ValidateOnStart`, propagate `IHostEnvironment` from `ApplicationModule`, add a non-Development fail-fast test and a Development-fallback warning test, add the new section to `appsettings.json` plus four per-environment files, document the new Key Vault secret in `environments.md`, run FR-6 verification greps, and finish with format + full build + full test sweep. Each task lists exact files, exact code, and exact commands with expected output. A spec-coverage map at the end traces every FR/NFR to the task that satisfies it.