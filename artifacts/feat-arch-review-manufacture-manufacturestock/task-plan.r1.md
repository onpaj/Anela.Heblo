Plan saved to `artifacts/feat-arch-review-manufacture-manufacturestock/plan.r1.md`.

The plan breaks the refactor into 9 TDD-driven tasks: scaffold the Manufacture-owned request/response/item DTOs (Tasks 1–2), write the ProductNotFound test first then a minimal handler (Task 3), drive happy-path sort/page/project from a failing test (Task 4), parameterised SortBy + SortDescending coverage to clear every switch branch (Task 5), repoint `ManufactureStockTakingController` and drop the Catalog `using` (Task 6), extend `ModuleBoundariesTests.ManufactureCatalogAllowlist` with three declaring-type entries plus a fallback loop for any compiler-generated leaks (Task 7), regenerate the TypeScript client and swap the imported type in `useManufactureStockTaking.ts` (Task 8), full verification + commit (Task 9). The Catalog use case directory, `StockTakingController`, Catalog tests, and `useStockTaking.ts` are explicitly verified untouched. A spec-coverage matrix at the bottom maps every FR/NFR and every arch-review amendment to specific tasks.