Spec written to `spec.md`. It expands the brief into 9 functional requirements (vector store, extractor, ingestion job, index handler, two-stage generation, REST, MCP, UI, config), 5 NFRs (perf P95 ≤12s, security, reliability, observability, code quality), the data model for both new tables, REST/MCP/UI interface contracts, dependency inventory, explicit out-of-scope, and 8 open questions flagged for the architect (markdown renderer choice, top-K tuning, Stage 2 embedding source, length enforcement, cold-start fallback, Czech labels, cron schedule, dedup granularity).