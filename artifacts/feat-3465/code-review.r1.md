## Review Result: CLEAN

### Blocking (correctness)
- None

### Advisory (cleanup)
- `backend/src/Anela.Heblo.Application/Features/Analytics/Services/MarginCalculator.cs:165-194` — `GetGroupAggregatedMarginData` recomputes `p.SalesHistory.Sum(s => s.AmountB2B + s.AmountB2C)` per product on every field (8 times per product) in the weighted-average branch, on top of the initial `totalSales` computation. Since this is a verbatim move from the handler, pre-existing, and out of scope per the spec (no algorithmic change intended), it isn't a defect introduced by this branch — but now that this logic has a single owner (`IMarginCalculator`) it would be a good follow-up to project `SalesHistory.Sum(...)` per product once (e.g. into a local `Dictionary<AnalyticsProduct, decimal>` or a tuple list) and reuse it across all eight weighted sums.
- `backend/src/Anela.Heblo.Application/Features/Analytics/Services/TopProductSorter.cs:28-69` — the `switch` on `sortBy.ToLower()` allocates a `List<TopProductDto>` via `.ToList()` independently in every branch with near-identical `OrderBy`/`OrderByDescending` pairs; a small helper taking a `Func<TopProductDto, IComparable>` key selector could collapse the 13 branches to one call site. Purely a readability/duplication nit, not a behavior concern, and matches the spec's explicit "verbatim move, no behavioral change" requirement, so it's advisory only.

### Notes
Verified the two extracted methods (`MarginCalculator.GetGroupAggregatedMarginData`, `TopProductSorter.Sort`) are byte-for-byte identical to the removed handler private methods (`CalculateGroupMarginData`, `ApplySorting`) by diffing the removed vs. added hunks line by line — no logic drift. `GroupMarginData` correctly relocated to `Services/GroupMarginData.cs` and changed from `internal` to `public`; no duplicate/stale copy remains anywhere else in the repo (`grep -r "class GroupMarginData"` returns only the new file). DI registration for `ITopProductSorter` added in `AnalyticsModule.cs` alongside the existing `IMarginCalculator`/`IMonthlyBreakdownGenerator` registrations, consistent with the module's pattern. Confirmed via repo-wide search (`grep -r "new GetProductMarginSummaryHandler"`) that `GetProductMarginSummaryHandlerTests.cs` is the only call site constructing the handler directly, and both construction points in that file (constructor-level real instance, and the mocked-`IMarginCalculator` test that also stubs `GetGroupAggregatedMarginData`) were updated for the new `ITopProductSorter` parameter. New unit tests in `MarginCalculatorTests.cs` and the new `TopProductSorterTests.cs` cover the acceptance criteria (empty list, zero-sales fallback, weighted average, all 13 sort keys x2 directions, null/empty/whitespace/unrecognized `sortBy`, case-insensitivity). The unrelated one-line fix in `GetConfigurationHandlerTests.cs` (`ConfigurationConstants.APP_VERSION` → `InfrastructureConfigurationKeys.APP_VERSION`) is verified legitimate: `ConfigurationConstants` has no `APP_VERSION` member, while production code in `GetConfigurationHandler.cs:75` reads `InfrastructureConfigurationKeys.APP_VERSION` — the old test reference was a genuine pre-existing compile error, now fixed to match the real key source. No API/contract changes; handler reduced to orchestration only per spec.
