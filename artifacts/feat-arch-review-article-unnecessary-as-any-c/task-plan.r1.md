Plan saved to `docs/superpowers/plans/2026-06-08-remove-unnecessary-as-any-useGetArticleQuery.md`.

**Summary:** Single-task plan covering the one-file edit at `frontend/src/api/hooks/useArticles.ts:173-185`. Steps verify starting state with grep, apply the exact before/after replacement (drops the `eslint-disable` comment, the `const raw = s as any` line, and the inline `as string | null` / `as number | null` assertions; switches to an implicit-return arrow function matching surrounding style), then re-verifies the cast is gone via grep, runs `npm run build`, `npm run lint`, and the existing `useArticles` tests, and commits as `refactor:`. All seven `??` fallbacks are preserved verbatim to satisfy FR-3. Self-review confirms every spec requirement maps to a concrete step.