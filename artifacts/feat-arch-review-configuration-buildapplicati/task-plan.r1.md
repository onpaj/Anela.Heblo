Plan saved to `docs/superpowers/plans/2026-06-03-remove-speculative-async-buildapplicationconfiguration.md`.

**Summary:** Three-task plan covering (1) baseline verification (file state, repo-wide grep confirms only two references, green build + tests), (2) the surgical refactor itself — single file `GetConfigurationHandler.cs`, change line 32 call site + lines 53–73 helper signature, with explicit CS1998/CS4014 guardrails and `dotnet format` whitespace-churn check — committed as one PR-ready commit, and (3) final validation gate (`dotnet build`, `dotnet format --verify-no-changes`, `GetConfigurationEndpointTests`, plus three grep-based sanity checks that prove the `Handle` MediatR contract is intact and zero `await`/`Task.CompletedTask` references remain in the file). Self-review confirms every spec requirement (FR-1..4, NFR-1..4) and every arch-review decision/amendment maps to a concrete task step.