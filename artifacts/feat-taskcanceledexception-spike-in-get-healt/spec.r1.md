Spec written to `artifacts/feat-taskcanceledexception-spike-in-get-healt/spec.md`. Five FRs cover the data-source rewire, explicit `OperationCanceledException` handling in `DataQualitySchemaHealthCheck`, a 5 s fail-fast probe timeout, telemetry-noise reduction targeted at the observed `NpgsqlWriteBuffer.Flush` problem ID, and contract preservation. Open questions flag the package-version overload, `UIResponseWriter`'s `Degraded`-to-status mapping, the Azure probe-timeout alignment, and the `Degraded`-vs-`Unhealthy` policy call.