## Module
Logistics

## Finding
`GiftPackageManufacture` is a sub-feature of Logistics, but `AddGiftPackageManufactureModule()` is called directly from `ApplicationModule.cs` (line 99), at the same level as `AddLogisticsModule()` (line 98). Similarly, `AddGiftSettingsModule()` is also called directly from `ApplicationModule` (line 118).

This exposes Logistics' internal sub-feature structure to the top-level composition root. Callers of `ApplicationModule` must know about — and remember to call — each internal sub-module individually. If another sub-feature is added to Logistics, someone must remember to add it to `ApplicationModule` separately; forgetting would silently break functionality.

## Why it matters
The documented DI pattern (development_guidelines.md, §Dependency Injection Patterns) says each module must have a single `{Feature}Module.cs` entry point. Splitting a module's DI wiring across multiple top-level calls at `ApplicationModule` violates module encapsulation: sub-feature internals leak through the composition root rather than being hidden behind the owning module's registration method.

## Suggested fix
Move both `AddGiftPackageManufactureModule()` and `AddGiftSettingsModule()` calls into `LogisticsModule.AddLogisticsModule()`:

```csharp
public static IServiceCollection AddLogisticsModule(this IServiceCollection services)
{
    // existing registrations ...
    services.AddGiftPackageManufactureModule();
    services.AddGiftSettingsModule();
    return services;
}
```

Then remove the two duplicate calls from `ApplicationModule.cs` lines 99 and 118.

---
_Filed by daily arch-review routine on 2026-09-05._

---
_Generated by [Claude Code](https://claude.ai/code)_
