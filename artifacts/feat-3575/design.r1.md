# Design: Invoices — revert tracked mutations on mid-import failure

## Skip Design: true

The architecture review (`arch-review.r1.md`) set `Skip Design: true` — this is a
backend-only EF Core change-tracker bug fix with no new or changed UI, screens,
layouts, or visual design decisions. No design document is required; this file
exists only to satisfy the planner's input contract.

See `arch-review.r1.md` for the full architectural guidance (component overview,
key design decisions, interfaces/contracts, and data flow) that the planner and
developer should follow.
