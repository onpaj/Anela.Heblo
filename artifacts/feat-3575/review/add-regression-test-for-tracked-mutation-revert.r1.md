# Code Review: add-regression-test-for-tracked-mutation-revert

## Summary
The new test `InvoiceImportRealChangeTrackerTests.cs` correctly exercises the real EF Core change tracker via a directly-constructed `ApplicationDbContext` + real `IssuedInvoiceRepository`, and genuinely targets the outer-catch bug path by making the mocked `IIssuedInvoiceImportTransformation.TransformAsync` throw for invoice A rather than `client.SaveAsync` — precisely the pitfall the task-context notes warned would exercise the wrong (in-scope, inner) catch block. I traced every mock setup line-by-line against the actual `InvoiceImportService.ExecuteImportInvoice`/`GetOrCreateAsync` control flow (commit `bea1c60d`) and `IssuedInvoiceRepository.RevertTrackedChangesAsync`, and the test's assumptions match the real signatures and behavior exactly.

## Review Result: PASS

### task: add-regression-test-for-tracked-mutation-revert
**Status:** PASS

## Docs to Update
None.

## Overall Notes
Verification performed:
- Read task-context, spec.r2.md (FR-4), arch-review.r1.md, the impl summary, and the actual test file in full.
- Read `InvoiceImportService.cs` and `IssuedInvoiceRepository.cs` as they exist on this branch (post-fix, commit `bea1c60d`) and traced the test's execution for both invoice A and B against the real code:
  - A: `GetByIdAsync` returns the tracked pre-existing row (`isNew=false`) → `_mapper.Map(detailA, invoice)` mutates it in place (matches the two-arg `Map(source, destination)` overload used at `InvoiceImportService.cs:93`, whose return value is discarded in production, so the mock's `Returns` is irrelevant and only the `Callback` mutation matters) → the single mocked `IIssuedInvoiceImportTransformation.TransformAsync(detailA, ...)` throws, which is *not* wrapped by the inner try (lines 102-116) and reaches the outer catch (lines 123-132) → `isNew==false` and `invoice != null`, so `RevertTrackedChangesAsync` runs → exception re-thrown → `ImportInvoicesAsync`'s per-invoice catch records `Failed`. The test's `_mockClient.Verify(..., Times.Never)` for A confirms `client.SaveAsync` is genuinely never reached, ruling out the inner-catch path the task-context explicitly said must not be used to simulate the bug.
  - B: `GetByIdAsync` returns null (`isNew=true`) → factory uses the single-arg `Map<TSource,TDestination>(detailB)` overload (correctly mocked separately from the two-arg overload) → `AddAsync`+`SaveChangesAsync` persists it → transformation passes through → `client.SaveAsync` succeeds → `SyncSucceeded` + `UpdateAsync`/`SaveChangesAsync` persists it again. This is the `SaveChangesAsync` call that would flush A's abandoned mutation if the revert hadn't run — exactly the scenario FR-4 requires.
  - Confirmed `EntityState.Unchanged` reset semantics (baseline-accept, not value-rollback) mean A's row is untouched only because no `SaveChangesAsync` occurred between the mutation and the revert — consistent with the arch-review's Decision 2 analysis, and the test's post-assertions (`AsNoTracking()` re-read after `ChangeTracker.Clear()`) correctly avoid reading back the still-tracked (now-"Unchanged"-but-mutated) in-memory object.
  - Confirmed domain/DTO shapes used by the test (`IssuedInvoice` fields, `ImportResultDto.Succeeded`/`Failed` as `List<string>`, `IssuedInvoiceRepository` constructor signature) match the real files.
- Confirmed via `git show 69592dc4` that the commit contains only the one new test file (126 lines added, no production code touched), consistent with this being a pure test-addition task.
- Could not independently re-execute `dotnet test` end-to-end in this review environment: the solution-wide `GenerateAccessMatrix` pre-build target (`Anela.Heblo.API.csproj:110-113`, `BeforeTargets="Build"`) invokes the unrelated `Anela.Heblo.AccessMatrixGen` tool, which crashed with an unhandled `JsonException` ("'/' is an invalid start of a value") while parsing `frontend/src/auth/accessMatrix.generated.ts`, aborting the build before the test project ran. This is a pre-existing, solution-wide build-tooling issue entirely unrelated to Invoices or this test file (confirmed by reading the target definition) — an environment gap in this review sandbox, not a defect introduced by this task. The developer's reported 28/28 pass and explicit fail-before/pass-after verification (temporarily neutralizing the outer-catch revert call via `if (false && !isNew && invoice != null)`, confirming the assertion flips to the mutated value) was therefore not re-executed live here. Static line-by-line tracing above independently corroborates that the test's logic would produce that exact fail/pass behavior, so this is noted as a residual verification gap caused by unrelated environment tooling, not a doubt about the test's correctness.
