Self-review complete: every spec requirement maps to a task, no placeholders, type/name references are consistent across tasks (verified `IStockValueService.GetStockValueChangesAsync` signature, `MonthlyStockChange` namespace, and both renamed test method names match in all references).

Plan saved to `docs/superpowers/plans/2026-06-08-remove-placeholder-stock-value-service.md`. It decomposes the work into 6 tasks ordered to keep the build green at every checkpoint: simplify DI registration → rename misleading factory-pattern test → rewrite override test with Moq → migrate integration-test factory to Moq → delete the dead placeholder → final grep/build/format/test verification. Each task includes exact file paths, line ranges, full code blocks for before/after states, commands with expected output, and a commit message.