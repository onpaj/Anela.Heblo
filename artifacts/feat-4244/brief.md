## Module
Configuration

## Finding
`GetConfigurationHandler.Handle()` wraps its entire body in a try-catch that only logs and immediately rethrows (`backend/src/Anela.Heblo.Application/Features/Configuration/GetConfigurationHandler.cs`, lines 27–48):

```csharp
try
{
    _logger.LogDebug("Handling GetConfiguration request");
    var appConfig = BuildApplicationConfiguration();
    // ...
    return response;
}
catch (Exception ex)
{
    _logger.LogError(ex, "Error retrieving application configuration");
    throw;
}
```

`BuildApplicationConfiguration()` only reads from `IConfiguration` (no I/O, no external calls) and constructs a value object — it cannot realistically throw. There is no recovery logic, no exception transformation, and no meaningful error signal added. The global exception handler (ASP.NET Core middleware) already logs unhandled exceptions before they surface as 500 responses.

## Why it matters
Dead catch blocks violate KISS: they add three lines of noise per handler, create false confidence that errors are handled, and will cause double-logging if the global handler also logs the same exception. Any future maintainer reading this handler has to reason about whether the catch clause does something meaningful before concluding it does not.

## Suggested fix
Remove the try-catch entirely. Let exceptions propagate to the global middleware:

```csharp
public async Task<GetConfigurationResponse> Handle(GetConfigurationRequest request, CancellationToken cancellationToken)
{
    _logger.LogDebug("Handling GetConfiguration request");

    var appConfig = BuildApplicationConfiguration();

    return new GetConfigurationResponse
    {
        Version = appConfig.Version,
        Environment = appConfig.Environment,
        UseMockAuth = appConfig.UseMockAuth,
        Timestamp = DateTime.UtcNow,
    };
}
```

---
_Filed by daily arch-review routine on 2026-09-21._

---
_Generated by [Claude Code](https://claude.ai/code)_
