## Module
OrgChart

## Finding
`frontend/src/pages/OrgChartPage.tsx` line 195: the outermost container `<div>` uses a hardcoded gradient without dark mode variants:

```tsx
<div className="h-screen bg-gradient-to-br from-indigo-500 to-purple-600 flex flex-col">
```

The inner chart canvas area (line 276) correctly uses `dark:from-graphite-bg dark:to-graphite-bg`, and the controls bar (line 197) uses `dark:bg-graphite-surface` etc. But the outermost full-viewport wrapper retains the bright indigo-to-purple gradient in dark mode with no `dark:` override.

## Why it matters
ADR-006 requires every component that renders color to work correctly in both light and dark mode. In dark mode (Graphite theme), the indigo/purple gradient background conflicts with the dark surface colors used by the children — the outer wrapper is visible at the bottom of the page during scroll, and any gap between children exposes the bright gradient, which breaks contrast and visual consistency.

## Suggested fix
Add a dark mode override to the outermost wrapper. Either remove the gradient in dark mode or switch to graphite tokens:

```tsx
<div className="h-screen bg-gradient-to-br from-indigo-500 to-purple-600 dark:from-graphite-bg dark:to-graphite-bg flex flex-col">
```

If the purple gradient is intentional for the light theme page chrome, this one-line change preserves the light theme and adds a proper dark equivalent.

---
_Filed by daily arch-review routine on 2026-09-12._

---
_Generated by [Claude Code](https://claude.ai/code)_
