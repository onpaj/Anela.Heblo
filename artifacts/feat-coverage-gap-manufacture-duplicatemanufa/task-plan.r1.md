Plan saved to `docs/superpowers/plans/2026-06-11-duplicate-manufacture-order-handler-tests.md`.

**Summary:** Five sequential tasks producing one new test file at `backend/test/Anela.Heblo.Tests/Features/Manufacture/DuplicateManufactureOrderHandlerTests.cs`. Task 1 scaffolds the class (constants, mocks, `BuildSourceOrder` helper). Tasks 2–4 add one `[Fact]` per FR (not-found, full duplication with semi-product, no semi-product), each followed by a focused test run and commit. Task 5 runs the full Manufacture suite, builds, formats, and verifies no production code was touched. All five architecture-review amendments are honored (flat path, `public class`, `Mock<TimeProvider>` over `FakeTimeProvider`, Moq, `FixedNow.UtcDateTime`). Lot/expiration assertions call the same static helpers the handler uses — no hardcoded format strings.