## Summary

`PUT TransportBox/ChangeTransportBoxState` is generating HTTP 409 Conflict responses — a spike of errors suggesting duplicate or concurrent state transitions on transport boxes.

## App Insights Evidence

- HTTP 409 spike observed in last 24h
- Endpoint: `PUT TransportBox/ChangeTransportBoxState`

## Root Cause

409 Conflict on a state change endpoint typically indicates either:
1. A duplicate active box code — two boxes share the same code and a uniqueness constraint is violated
2. A concurrent update conflict — two requests attempt to change the same box's state simultaneously and one loses the optimistic concurrency check
3. An invalid state transition being attempted (e.g. transitioning from a terminal state)

## Proposed Fix

1. Review the `ChangeTransportBoxState` handler and identify the exact source of the 409 response.
2. If a uniqueness constraint: audit how box codes are generated and enforce uniqueness at creation time.
3. If a concurrency conflict: implement retry with exponential backoff or use a distributed lock for state transitions.
4. Add structured logging to capture the box ID and conflict reason when a 409 occurs to aid diagnosis.

---
*Auto-generated by nightly App Insights analysis*