Plan saved to `docs/superpowers/plans/2026-06-01-decouple-ireportbuilderservice.md`.

**Summary:** 9 tasks. Tasks 1–3 add the three contract DTOs (transcribed verbatim from the existing nested response classes). Task 4 swaps `IReportBuilderService` + `ReportBuilderService` to return them — intentionally leaves the build red. Task 5 adds an inline `Select` projection in `GetProductMarginAnalysisHandler.BuildSuccessResponse`. Task 6 switches the internal `ReportData` helper in `GetMarginReportHandler` to DTO lists and adds a single projection seam in `BuildSuccessResponse` (so the M2Percentage sort still runs on the DTO before projection). Task 7 updates the two handler test mocks (type-name swaps only). Tasks 8 and 9 verify the dependency-direction invariant, the empty `git diff` on generated OpenAPI + TS clients, `dotnet format` cleanliness, and the full test run.