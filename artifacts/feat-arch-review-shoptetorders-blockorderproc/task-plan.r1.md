Plan saved to `artifacts/feat-arch-review-shoptetorders-blockorderproc/plan.r1.md` — 7 tasks covering RED tests (Tasks 1–4: three new failing tests for the two partial-success paths and cancellation propagation), GREEN implementation (Task 5: split handler into outer status-commit catch and inner best-effort remark catch with `when (ex is not OperationCanceledException)`), verification, and commit. All four FRs and all four arch amendments (A-1 through A-4) are mapped to specific tasks in the self-review.