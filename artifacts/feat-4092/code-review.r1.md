## Review Result: CHANGES_REQUESTED

### Blocking (correctness)
- `backend/test/Anela.Heblo.Tests/Features/BackgroundJobs/RecurringJobNextRunCalculatorTests.cs:57-65` and `:84-92` — `Calculate_ReturnsNull_AndLogsWarning_WhenTimezoneUnknown` and `Calculate_ReturnsNull_AndLogsWarning_WhenCronInvalid` verify only `LogLevel.Warning` + `Times.Once`, using `It.Is<It.IsAnyType>((state, t) => true)` — a predicate that matches any log state. This does not verify FR-2/FR-3's explicit acceptance criteria that the warning "reference the supplied `timeZoneId`/`cronExpression` and `jobName`" (spec.r1.md lines 21, 28). A regression that logs a warning with an unrelated or empty message (e.g. drops the timezone id or job name from the template) would still pass this test — the message content is never asserted, only that *some* warning fired. This is not a hypothetical: the sibling test file in the very same directory (`GetRecurringJobsListHandlerTests.cs:259-265`) and `UpdateRecurringJobStatusHandlerTests.cs:302-308`) establish the exact pattern needed here — `It.Is<It.IsAnyType>((v, t) => v.ToString()!.Contains("NOT_A_CRON") && v.ToString()!.Contains("Job1"))` — which this new test should follow (asserting the log message contains the supplied `timeZoneId`/`cronExpression` value and `"test-job"`). As written this is a vacuous assertion relative to the spec's stated acceptance criteria and NFR-3's "consistency with existing conventions" requirement, and should be fixed before merge.

### Advisory (cleanup)
- None

### Verification notes (not findings, recorded for confidence)
- Verified independently via `zoneinfo`/`calendar` (not the test's own comments): 2026-03-29 is the last Sunday of March (Europe/Prague spring-forward, 02:00 CET → 03:00 CEST at 01:00 UTC) and 2026-10-25 is the last Sunday of October (fall-back, 03:00 CEST → 02:00 CET at 01:00 UTC). Both DST test inputs (`utcNow` values and the `"30 2 * * *"` CRON) genuinely land the computed `nextLocal` inside the respective gap/ambiguous-hour window, and the asserted UTC instants (`2026-10-25T01:30:00Z` for the ambiguous-hour case) correctly reflect standard-offset (CET, UTC+1) resolution rather than the DST offset (which would have produced `00:30:00Z` instead) — so the DST assertions genuinely discriminate DST-vs-standard resolution rather than being tautological.
- The non-UTC happy-path test (`Calculate_ReturnsExpectedUtcInstant_ForNonUtcTimezone`) asserts `05:00:00Z`, which differs from what a broken (timezone-ignoring) implementation would produce (`06:00:00Z`), so it genuinely exercises the local-time round trip rather than degenerating to the UTC case.
- The invalid-CRON test's input (`"not a cron"`, 3 whitespace-separated tokens) is consistent with other already-passing tests in this codebase (`GetRecurringJobsListHandlerTests.cs` using `"NOT_A_CRON"`/`"INVALID_CRON"`, `UpdateRecurringJobCronHandlerTests.cs` using `"not-a-cron"`) that rely on `CrontabSchedule.Parse` throwing `CrontabException` for malformed field counts, so it genuinely reaches and exercises that catch block rather than failing earlier.
