## Module / File
backend/src/Anela.Heblo.Domain/Features/InvoiceClassification/Rules/VatClassificationRule.cs

## Coverage
Line coverage: 0% (filter threshold: 60%)

## What's not tested
The `Evaluate` method performs a case-insensitive, trim-on-both-sides comparison of the invoice's `CompanyVat` against a stored pattern. No test verifies: (1) a matching IČO returns true, (2) a non-matching IČO returns false, (3) whitespace-only differences are ignored correctly, (4) a null `CompanyVat` or null `pattern` does not throw.

## Why it matters
This is a live classification rule used to automatically route incoming supplier invoices to accounting templates. A silent regression here could misclassify every invoice from a vendor identified by IČO, sending them to the wrong accounting account with no immediate alert.

## Suggested approach
Unit-test `VatClassificationRule.Evaluate` with: exact match, case mismatch, leading/trailing whitespace variants, null CompanyVat, null pattern, and a definitely non-matching IČO. All should be deterministic and require no infrastructure — pure unit tests. ~1 hour of effort.

---
_Filed by weekly coverage-gap routine on 2026-08-24. Based on CI run #32143334687 (507972a7a76bc1a3ef1f46762b275e69100c9f37)._

---
_Generated by [Claude Code](https://claude.ai/code)_
