Plan saved to `docs/superpowers/plans/2026-06-03-inject-timeprovider-getproductmarginshandler.md`.

**Summary:** One task with ten bite-sized TDD steps — create test file with the first failing test (compile-time fail), modify the handler to add `TimeProvider` injection (field + ctor param + one expression swap at line 189), add the timezone-boundary test, run the Catalog test slice, format, build, commit. Two unit tests pin the new contract: a 13-month boundary inclusion/exclusion case and a UTC-vs-UTC+1 day-boundary case (per arch-review amendment #2). No DI registration changes needed — `TimeProvider.System` is already singleton-registered. No new test dependencies; uses `Mock<TimeProvider>` to match the sibling `GetCatalogDetailHandlerTests` pattern. Self-review confirms full spec/arch coverage.