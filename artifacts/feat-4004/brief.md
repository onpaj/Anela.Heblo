## Module
Journal

## Finding
`IJournalRepository.GetEntriesByProductAsync` (`backend/src/Anela.Heblo.Domain/Features/Journal/IJournalRepository.cs`, lines 27–30) is declared on the domain interface and implemented in `JournalRepository` (`backend/src/Anela.Heblo.Persistence/Journal/JournalRepository.cs`, lines 139–151), but no MediatR handler or controller action calls it. The feature that it was presumably designed for — surfacing journal entries for a product on the catalog detail page — is instead served by `SearchJournalEntriesHandler` passing a `productCodePrefix` argument (see `useJournalEntriesByProduct` in `frontend/src/api/hooks/useJournal.ts`, lines 191–215). The method is exercised only by integration tests (`JournalRepositoryIntegrationTests.cs`).

## Why it matters
A method on a domain repository interface is a public contract: any future implementation (an in-memory fake, a different DB adapter) must provide it. Carrying it with no production consumer violates the Interface Segregation Principle and YAGNI — the surface area of the abstraction grows without adding value, increasing the cost of every future implementation or test double that must satisfy it.

## Suggested fix
Remove `GetEntriesByProductAsync` from `IJournalRepository` (line 27–30) and its implementation from `JournalRepository` (lines 139–151). Delete the corresponding integration test cases in `JournalRepositoryIntegrationTests.cs`. The `productCodePrefix` filter on `SearchEntriesAsync` already covers the use case.

If a dedicated by-product endpoint is planned, the correct order is to implement the handler first, then add only what the handler requires to the interface.

---
_Filed by daily arch-review routine on 2026-08-31._

---
_Generated by [Claude Code](https://claude.ai/code)_
