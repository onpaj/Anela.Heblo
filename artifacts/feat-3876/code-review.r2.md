## Review Result: CLEAN

### Blocking (correctness)
- None

### Advisory (cleanup)
- `frontend/src/components/customer-support/smartsupp/hooks/useGenerateDraftReply.ts:74`, `frontend/src/components/customer-support/smartsupp/hooks/useSendMessage.ts:54`, `frontend/src/api/hooks/useSmartsupp.ts:122` — All three `catch` blocks still duplicate the identical `(e as { errorCode?: string }).errorCode as ErrorCodes | undefined` narrowing (a fourth copy already exists in `useSubmitDraftReplyFeedback.ts:23-24`). A shared `extractErrorCode(e: unknown): ErrorCodes | undefined` helper would remove the duplication. This was already flagged in round 1 and intentionally deferred; still valid.
- `frontend/src/components/customer-support/smartsupp/hooks/useGenerateDraftReply.ts:74`, `useSendMessage.ts:54`, `frontend/src/api/hooks/useSmartsupp.ts:126` — The `if (!data.success)` branches immediately below each fixed `catch` are dead code: `BaseApiController.HandleResponse` never returns HTTP 200 with `success: false` for these endpoints, so `data.success` is always `true` on the non-throwing path. Harmless as defensive code, but no test can exercise it against real backend behavior (the unit tests exercise it only via a mocked resolved value that doesn't reflect what the backend actually sends).
- `frontend/src/components/customer-support/smartsupp/hooks/__tests__/useGenerateDraftReply.test.ts`, `useSendMessage.test.ts`, `frontend/src/api/hooks/__tests__/useCloseConversation.test.ts` — None of the "throws" test cases reject with an error object carrying `errorCode` (all use plain `new Error("boom")`), so the round-1 regression (specific Czech messages becoming unreachable via the catch path) would not have been caught by these tests and the round-2 fix isn't regression-guarded by them either. Worth adding one case per hook that rejects with `{ errorCode: ErrorCodes.SmartsuppConversationNotFound }` and asserts the specific message surfaces.

### Verification notes
Re-derived the round-1 bug mechanism independently against the current generated client (`frontend/src/api/generated/api-client.ts`) rather than trusting the fix summary:
- `processSmartsupp_GenerateDraftReply` / `processSmartsupp_SendMessage` / `processSmartsupp_CloseConversation` all route their 400/404 (and 403/409 for feedback) branches through `ProblemDetails.fromJS(...)` then `throwException(...)`.
- `throwException` does `throw result` when `result` is non-null — i.e. it throws the `ProblemDetails` instance itself, not an `ApiException` wrapper with a nested `.result`.
- `ProblemDetails.init()` blanket-copies every property of the parsed JSON body (`for (var property in _data) this[property] = _data[property]`) onto the instance, so the backend's `{success:false, errorCode:"..."}` body's `errorCode` field lands as a top-level own property on the thrown object.
- `BaseResponse.errorCode` (parent of `GenerateDraftReplyResponse`/`SendMessageResponse`/`CloseConversationResponse`) is already typed `ErrorCodes | undefined`, and the `ErrorCodes` enum values (`SmartsuppConversationNotFound`, `SmartsuppDraftReplyAiUnavailable`, `SmartsuppConversationEmpty`, `SmartsuppSendMessageUnavailable`, `SmartsuppCloseConversationUnavailable`, `SmartsuppDraftReplyFeedbackLogNotFound`, `SmartsuppDraftReplyFeedbackAlreadySubmitted`) match the string keys used in each hook's message-mapping table.
- All three `catch` blocks now read `(e as { errorCode?: string }).errorCode` and pass it through to `messageForError`/`messageForSendError`/`messageForCloseError`, whose lookup tables contain the specific Czech messages — confirmed these branches are genuinely reachable, not just plausible.
- Cross-checked `useSubmitDraftReplyFeedback.ts` (same PR, not part of round-1's findings): its 403/409 discrimination by `errorCode` (`SmartsuppDraftReplyFeedbackLogNotFound` / `SmartsuppDraftReplyFeedbackAlreadySubmitted`, both `[HttpStatusCode(HttpStatusCode.Conflict)]` per `backend/src/Anela.Heblo.Application/Shared/ErrorCodes.cs:343-346`) is correct and consistent with the same `ProblemDetails` blanket-copy mechanism.
- Swept the rest of the diff (all five migrated hooks, `useSmartsupp.ts`'s Shoptet/visitor-info escape-hatch hooks, `usePresenceHeartbeat`'s typed-heartbeat/escape-hatch-unmount split, `smartsuppClient.ts` deletion, the null/undefined-safety fallout across `ContactDetailsPanel.tsx`/`ConversationDetail.tsx`/`ConversationList.tsx`/`MessageBubble.tsx`/`ShoptetCustomerCard.tsx`/`VisitorInfoCard.tsx`/`StatusPill.tsx`, and the shared `ragFeedbackMapping.ts` extraction) — no other correctness bugs found with high confidence. `smartsuppClient`/`asInternal` have no remaining production references.
