**Self-review complete.** Spec coverage map: all 7 FRs and 4 NFRs are mapped to specific tasks in the Acceptance Mapping table. All 6 arch-review amendments are addressed (resolved `userId` passed to `onTileMissing`, XML invariants on interface, `string? userId` parameter, deadlock warning copied to interface remarks, explicit no-new-MutatorTests reminder, `SaveUserSettingsHandler` follow-up noted). No placeholders; every code step shows the full literal contents. Type consistency verified: `MutateAsync` signature in Task 1 matches the implementation in Task 2 and both closure shapes in Tasks 4 & 5. One non-obvious decision called out prominently: both handlers are flipped from `public` to `internal sealed` because CS0051 forbids a public class taking an internal interface as a constructor parameter — precedent is `Features/FeatureFlags/**/*Handler.cs`.

Plan saved to `docs/superpowers/plans/2026-06-03-dashboard-tile-mutator-refactor.md`. Six tasks: (1) add interface + result struct, (2) implement mutator, (3) DI registration, (4) atomic `EnableTileHandler` + test ctor rewrite, (5) atomic `DisableTileHandler` + test ctor rewrite, (6) full verification including OpenAPI byte-identity check.