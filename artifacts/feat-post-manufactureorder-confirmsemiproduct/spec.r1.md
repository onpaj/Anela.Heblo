Spec written to `artifacts/feat-post-manufactureorder-confirmsemiproduct/spec.md`. Six functional requirements (outcome classification, error-code propagation, FlexiBee → 502 mapping, structured logging, App Insights dimensions, backwards compatibility), four NFRs, contract tables for the HTTP behavior change, and six open questions — most importantly whether HTTP 200 + `ManualErpActionRequired` is the right shape for soft-failure ERP cases (today's silent 400) and whether any non-frontend consumers branch on the 400 status.