## Module
UserManagement

## Finding
Two exception types live in the `Contracts/` subfolder of the UserManagement application feature:

- `backend/src/Anela.Heblo.Application/Features/UserManagement/Contracts/GraphServiceAuthException.cs` (lines 1–14)
- `backend/src/Anela.Heblo.Application/Features/UserManagement/Contracts/GraphServiceException.cs` (lines 1–14)

`Contracts/` is documented in `filesystem.md` as the home for "Shared DTOs across use cases" (`{Entity}Dto.cs`, `[Other shared DTOs]`). These are exception classes — not DTOs — that represent external-service failures from `IGraphService`. The documented location for exceptions in a complex feature is `Infrastructure/Exceptions/`.

They are referenced by:
- `IGraphService` (doc comments), in `Services/`
- `GetGroupMembersHandler.cs` (catch blocks), in `UseCases/GetGroupMembers/`
- `EntraAccessUserSourceAdapter.cs` (catch blocks), in `Infrastructure/`
- `GraphArticleUserResolver.cs` (catch blocks), in `Infrastructure/`

## Why it matters
The `Contracts/` folder's semantic meaning (DTOs) is diluted by hosting exception types. A developer handling `IGraphService` errors or searching for exception types will look in `Infrastructure/Exceptions/` per the documented layout, not in `Contracts/`. This creates a discoverability gap and sets a precedent to put non-DTO types in `Contracts/`.

## Suggested fix
Move both files to a new folder `backend/src/Anela.Heblo.Application/Features/UserManagement/Exceptions/` (or `Infrastructure/Exceptions/`), update their namespace accordingly (e.g. `Anela.Heblo.Application.Features.UserManagement.Exceptions`), and fix the four `using` statements in the files that reference them. No logic changes required.

---
_Filed by daily arch-review routine on 2026-09-23._

---
_Generated by [Claude Code](https://claude.ai/code)_
