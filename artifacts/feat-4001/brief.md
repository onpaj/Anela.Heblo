telemetry-signal: exception:System.NullReferenceException@McpProductNotFoundTelemetryFilter.Process

## Signal

`System.NullReferenceException` at `Anela.Heblo.API.Telemetry.McpProductNotFoundTelemetryFilter.Process` — 6 occurrences in the last 7 days (window 2026-08-23 – 2026-08-30), all clustered in the last ~31 hours:

| timestamp (UTC) |
|---|
| 2026-08-29T13:35:20.69 |
| 2026-08-30T14:25:29.11 |
| 2026-08-30T15:20:33.35 |
| 2026-08-30T16:25:24.57 |
| 2026-08-30T17:45:22.07 |
| 2026-08-30T20:15:24.89 |

Zero occurrences before 2026-08-29 in the queried window — this is a new signal, not a pre-existing chronic one.

## Root cause (read from source)

`backend/src/Anela.Heblo.API/Infrastructure/Telemetry/McpProductNotFoundTelemetryFilter.cs:26-29`:

```csharp
public void Process(ITelemetry item)
{
    if (item is ExceptionTelemetry exc
        && exc.Message.Contains(ProductNotFoundMarker, StringComparison.Ordinal)
        && IsMcpException(exc))
```

This `ITelemetryProcessor` runs against **every** `ExceptionTelemetry` item flowing through the pipeline (not just MCP ones — the type check happens after the `Message.Contains` call). `ExceptionTelemetry.Message` is nullable; if any exception telemetry item reaches this processor with `Message == null`, `.Contains(...)` throws `NullReferenceException`, and since this filter is presumably part of a chained `ITelemetryProcessor` pipeline, an exception here can disrupt processing of that telemetry item (and potentially the chain) instead of just passing it through.

## Correlation hypothesis

All 6 timestamps line up (within ~50ms) with a `System.InvalidOperationException` at `Anela.Heblo.Adapters.Plaud.PlaudCliClient+<RunCliCoreAsync>d__9.MoveNext` tracked at the same instant (filed separately — see companion issue for `exception:System.InvalidOperationException@PlaudCliClient.RunCliCoreAsync`). Hypothesis: whatever code path tracks the Plaud CLI failure as `ExceptionTelemetry` constructs or forwards an item with `Message == null`, tripping this filter. Not yet confirmed which call site produces the null `Message`.

## Next step

1. Null-guard the check: `exc.Message?.Contains(ProductNotFoundMarker, StringComparison.Ordinal) == true` (minimal, unconditionally safe fix regardless of root cause).
2. Add a regression case to the existing `McpProductNotFoundTelemetryFilterTests.cs` for an `ExceptionTelemetry` with `Message == null`.
3. Separately trace what constructs a null-`Message` `ExceptionTelemetry` (likely wherever the Plaud CLI job's failure is tracked) — fixing only the null-check treats the symptom, not why `Message` is null in the first place.

---
_Generated by [Claude Code](https://claude.ai/code)_
