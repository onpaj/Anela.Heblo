## Review Result: CLEAN

### Blocking (correctness)
- None

### Advisory (cleanup)
- `backend/src/Anela.Heblo.Domain/Features/Logistics/Transport/TransportBoxStateRules.cs:28` — `CodeReleasingStates` is `private` rather than the `public static readonly TransportBoxState[]` the spec's Interface Design section specifies. This is a deliberate, well-reasoned deviation (documented inline: a public array element is mutable and could silently reopen the bug), and no test or consumer references it directly, so it does not block. Worth a one-line note back to whoever owns the spec so the interface doc and the code stay in sync.
- `backend/src/Anela.Heblo.Persistence/Logistics/TransportBoxes/TransportBoxRepository.cs:117` — `GetByCodeAsync` implements the "occupying boxes first" ordering as `OrderByDescending(TransportBoxStateRules.OccupiesCodePredicate)` rather than the literal `CodeReleasingStates.Contains(...) ? 1 : 0` ascending sketch in FR-5. Functionally equivalent (and arguably better — it reuses the shared predicate instead of restating the partition, closer to the spirit of FR-1's "no allow/deny-list elsewhere" acceptance criterion) and covered by `GetByCodeAsync_StockedBoxWithHigherId_ReturnsOccupyingOpenedBox` / `GetByCodeAsync_OnlyReleasedBoxes_ReturnsNewest`. No action needed.

## Notes

- Full feature diff (4372 lines across 35 files) reviewed against `spec.r1.md`. Real code changes are limited to 4 files: `OpenOrResumeBoxByCodeHandler.cs`, `ITransportBoxRepository.cs` (XML doc only), the new `TransportBoxStateRules.cs`, and `TransportBoxRepository.cs`; the remainder is tests, pipeline artifacts, and a memory gotcha doc.
- Verified `dotnet build` on `Anela.Heblo.sln`: 0 errors (252 pre-existing warnings, none touching changed files).
- Verified `dotnet format --verify-no-changes` on the four changed source files: clean.
- Ran the relevant test filter (`TransportBox|OpenOrResumeBoxByCode`): 233 passed, 5 failed. All 5 failures are pre-existing Testcontainers/Docker-unavailable environment failures (`System.ArgumentException: Docker is either not running or misconfigured`), affecting both the new `TransportBoxRepositoryCodeOccupancySqlShapeTests` (3 tests, correctly gated behind a real Postgres container per FR's "SQL shape" intent) and an unrelated pre-existing integration test (`ChangeTransportBoxStateReceiveAtomicityIntegrationTests`, 2 tests) — not a regression introduced by this change. All in-memory-DB and unit tests relevant to this feature passed, including `TransportBoxStateRulesTests` (all 10 enum members classified, predicate/function agreement), the new `TransportBoxUniquenessTests` Quarantine/Error cases, the new `OpenOrResumeBoxByCodeHandlerTests` busy-state cases, and the new `TransportBoxRepositoryCaseHandlingTests` per-state truth table.
- Traced each FR against the diff: FR-1 (`TransportBoxStateRules` type, Domain-only, no Application/Persistence dependency) — met. FR-2 (`IsBoxCodeActiveAsync` single `AnyAsync`, no local array) — met, verified no client-side evaluation. FR-3 (`HandleNewToOpened` unchanged, behavior pinned by new tests) — met, no code touched there as the spec expects. FR-4 (`OpenOrResumeBoxByCodeHandler` three-branch structure preserved, deny-list replaced by `OccupiesCode`) — met. FR-5 (`GetByCodeAsync` reorders on occupancy then `Id` descending, `Include`s and debug log untouched) — met. FR-6 (drift-guard test enumerates `Enum.GetValues<TransportBoxState>()`, not a hard-coded list) — met. FR-7 (memory gotcha file with both call sites and verbatim read-only detection SQL) — met.
- Confirmed no leftover literal `TransportBoxState.Closed` / `TransportBoxState.Stocked` comparisons for code-uniqueness purposes at the two consuming call sites (`grep` shows the only remaining direct enum comparisons are inside `TransportBoxStateRules.cs` itself and the deliberately-out-of-scope `isActiveFilter` / `isReceivable` filters called out in the spec).
- No DTO/contract changes, no OpenAPI regeneration needed, consistent with NFR-2. No frontend files touched, consistent with the spec's stated test scope.
