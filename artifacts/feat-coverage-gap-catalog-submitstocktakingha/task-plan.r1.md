Plan saved to `artifacts/feat-coverage-gap-catalog-submitstocktakingha/plan.r1.md`. It breaks the work into 6 bite-sized tasks (scaffold → FR-1 error path → FR-2 silent-correctness regression guard → FR-3 happy path with real `SyncStockTaking` → FR-4 exception path → validation/format), each with code, expected commands, and a commit step. All four spec FRs, three architecture-review amendments, and the NFRs map to specific tasks in the coverage table at the bottom.