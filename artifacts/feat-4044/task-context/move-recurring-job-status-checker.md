### task: move-recurring-job-status-checker

**Goal:** Move `RecurringJobStatusChecker.cs` from the `BackgroundJobs` module root into `BackgroundJobs/Services/`, update its namespace to match, and fix the resulting stale reference — with zero behavior change.

**Context (self-contained — no other files need to be read to do this work):**

The Application-layer class `RecurringJobStatusChecker` (which implements the Domain interface `IRecurringJobStatusChecker` from `Anela.Heblo.Domain.Features.BackgroundJobs`) currently sits at the `BackgroundJobs` module root, unlike its sibling service implementations (`RecurringJobSeeder`, etc.) which all live in `BackgroundJobs/Services/` under the `.Services` namespace. This task brings it in line with that established convention.

**Exact changes to make:**

1. **Move the file** (git move, preserving history where practical):
   - From: `backend/src/Anela.Heblo.Application/Features/BackgroundJobs/RecurringJobStatusChecker.cs`
   - To: `backend/src/Anela.Heblo.Application/Features/BackgroundJobs/Services/RecurringJobStatusChecker.cs`

2. **Change the namespace declaration inside the moved file:**
   - From: `namespace Anela.Heblo.Application.Features.BackgroundJobs;`
   - To: `namespace Anela.Heblo.Application.Features.BackgroundJobs.Services;`
   - (Match whichever namespace syntax — file-scoped `namespace X;` or block-scoped `namespace X { }` — the file currently uses; do not change the style, only the namespace value.)
   - Do NOT change anything else in this file: class name, constructor, method signatures, method bodies, dependencies, and its implementation of `IRecurringJobStatusChecker` must remain byte-for-byte identical apart from the namespace line and any using directives the moved file itself needs (its own usings should not need to change, since it moves within the same project and its dependencies — e.g. the Domain interface — are referenced via `using` statements, not relative namespace, and remain valid from the new location).

3. **Update the stale `using` directive in the test file:**
   - File: `backend/test/Anela.Heblo.Tests/Features/BackgroundJobs/RecurringJobStatusCheckerTests.cs`
   - Change: `using Anela.Heblo.Application.Features.BackgroundJobs;` → `using Anela.Heblo.Application.Features.BackgroundJobs.Services;`
   - If the test file references any *other* type from the old `Anela.Heblo.Application.Features.BackgroundJobs` namespace (root-level, e.g. `RecurringJobNextRunCalculator` or module registration types) in addition to `RecurringJobStatusChecker`, keep both `using` statements (add the new one rather than replacing, and verify with `dotnet build` that nothing is left unresolved). Based on direct inspection during planning, this file currently references only `RecurringJobStatusChecker` from that namespace, so a straight replace is expected to be correct — but confirm via the build in the verification step below.
   - Do not change any test method bodies, assertions, or test data — this is a `using`-only change.

4. **Re-verify (do not blindly edit) `BackgroundJobsModule.cs`:**
   - File: `backend/src/Anela.Heblo.Application/Features/BackgroundJobs/BackgroundJobsModule.cs`
   - This file's own namespace is the module root (`Anela.Heblo.Application.Features.BackgroundJobs`), and it already contains `using Anela.Heblo.Application.Features.BackgroundJobs.Services;` (needed today for `RecurringJobSeeder`). The DI registration line `services.AddScoped<IRecurringJobStatusChecker, RecurringJobStatusChecker>();` is expected to keep resolving correctly after the move with **no edit needed** to this file.
   - Action: do not edit this file speculatively. Only touch it if `dotnet build` (step 5) reports an unresolved `RecurringJobStatusChecker` reference here — in that case, add `using Anela.Heblo.Application.Features.BackgroundJobs.Services;` if it is somehow missing (it should already be present).

5. **Search for any other stale reference** to the old namespace for this specific type, to catch anything not already identified:
   - Run: `grep -rn "RecurringJobStatusChecker" backend/ --include=*.cs` and confirm every match is either (a) the moved file itself, (b) `RecurringJobStatusCheckerTests.cs` (handled in step 3), (c) `BackgroundJobsModule.cs` (handled in step 4), or (d) a reference to the Domain interface `IRecurringJobStatusChecker` (unaffected — its namespace `Anela.Heblo.Domain.Features.BackgroundJobs` does not change). Known interface-only consumers include `PurchasePriceRecalculationJob.cs`, `InvoiceDqtJob.cs`, `LotStockReconciliationDqtJob.cs`, `ProductPairingDqtJob.cs`, and similar — these need no changes.
   - If any other file surfaces referencing the concrete class via the old namespace, add/update its `using` directive the same way as step 3.

**Explicitly out of scope:**
- No change to `IRecurringJobStatusChecker` (Domain layer, `Anela.Heblo.Domain.Features.BackgroundJobs`).
- No change to any other file in the `BackgroundJobs` module besides the ones named above.
- No logic, method signature, constructor, or DI registration behavior changes.

**Verification commands (run from repo root, `/home/user/worktrees/feature-4044-Arch-Review-Backgroundjobs-Recurringjobstatuscheck`):**

1. Build the whole solution and confirm no errors/new warnings:
   ```
   dotnet build Anela.Heblo.sln
   ```
2. Format check on the touched files (repo-wide format is fine given the tiny diff):
   ```
   dotnet format Anela.Heblo.sln
   ```
   Confirm it reports no remaining issues (re-run `dotnet build` afterward if `dotnet format` rewrites anything).
3. Run the specific test class affected by this change:
   ```
   dotnet test backend/test/Anela.Heblo.Tests/Anela.Heblo.Tests.csproj --filter "FullyQualifiedName~RecurringJobStatusCheckerTests"
   ```
   All tests in `RecurringJobStatusCheckerTests` must pass unchanged (same assertions, same outcomes — only compiled against the new namespace).
4. Run the full test suite to confirm no other test was broken by the move:
   ```
   dotnet test backend/test/Anela.Heblo.Tests/Anela.Heblo.Tests.csproj
   ```

**Definition of done:**
- `backend/src/Anela.Heblo.Application/Features/BackgroundJobs/RecurringJobStatusChecker.cs` no longer exists.
- `backend/src/Anela.Heblo.Application/Features/BackgroundJobs/Services/RecurringJobStatusChecker.cs` exists with namespace `Anela.Heblo.Application.Features.BackgroundJobs.Services` and otherwise-unchanged content.
- `RecurringJobStatusCheckerTests.cs` compiles against the new namespace with no assertion changes.
- `dotnet build`, `dotnet format`, and the full `dotnet test` run (including `RecurringJobStatusCheckerTests`) all succeed with no failures introduced by this change.
- `grep -rn "RecurringJobStatusChecker" backend/ --include=*.cs` shows no file left referencing the concrete type through the old namespace.
