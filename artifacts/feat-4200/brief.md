## Module
Purchase

## Finding
`GetPurchaseStockAnalysisHandler` (215 lines) contains four private business-logic methods that should live in `IStockAnalysisCalculator` or a dedicated service, not in the handler:

| Method | Lines | Concern |
|---|---|---|
| `AnalyzeStockItem()` | 94–145 | Per-item analysis: calls two service methods, computes dailyConsumption, daysUntilStockout, optimalStock, then maps the result |
| `ShouldIncludeItem()` | 166–180 | Stock-status and configuration filter rules |
| `SortItems()` | 184–197 | Sort-by enum → LINQ ordering, with descending reversal |
| `CalculateSummary()` | 200–214 | Severity counts and total inventory value over all items |

`IStockAnalysisCalculator` (`backend/src/Anela.Heblo.Application/Features/Purchase/Services/IStockAnalysisCalculator.cs`) currently exposes only two pure-math helpers (`CalculateStockEfficiency`, `CalculateRecommendedOrderQuantity`). The handler delegates those two calculations to the service, then does all higher-level orchestration itself.

File: `backend/src/Anela.Heblo.Application/Features/Purchase/UseCases/GetPurchaseStockAnalysis/GetPurchaseStockAnalysisHandler.cs`, lines 94–214

## Why it matters
- **SRP**: The handler is responsible for data retrieval, per-item analysis, status filtering, text search, sorting, pagination, and summary calculation — seven distinct concerns in one class.
- **Testability**: The analysis rules (severity thresholds, filter conditions, sort order) can only be exercised via the full handler under test. Moving them to the calculator service enables focused, fast unit tests.
- **Maintainability**: Adding a new filter predicate or sort key requires editing the 215-line handler. The file is already approaching a size where navigation and review cost rises.

## Suggested fix
Move `AnalyzeStockItem`, `ShouldIncludeItem`, `SortItems`, and `CalculateSummary` into `IStockAnalysisCalculator` / `StockAnalysisCalculator` (possibly as a new overload or a dedicated `Analyze(IEnumerable<MaterialStockSnapshot>, GetPurchaseStockAnalysisRequest)` method). The handler becomes a thin coordinator:

```csharp
var snapshots = await _materialCatalog.GetStockAnalysisSnapshotsAsync(fromDate, toDate, cancellationToken);
var result = _stockAnalysisCalculator.Analyze(snapshots, fromDate, toDate, request);
// paginate result.Items and return
```

This is the minimal change: no new abstractions, just moving existing private methods into the service that already owns stock-level mathematics.

---
_Filed by daily arch-review routine on 2026-09-15._

---
_Generated by [Claude Code](https://claude.ai/code)_