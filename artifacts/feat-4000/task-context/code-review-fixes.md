## Goal
Fix the code review findings below

### Blocking (correctness)
- `frontend/src/pages/ExpeditionListArchivePage.tsx:104-114` (paired with `frontend/src/components/pages/ExpeditionListArchive/ExpeditionJobControlsBar.tsx:2746-2818`) — the "Obnovit" button's position in the header row changed. Before the refactor, all four action buttons lived in one `flex gap-2` div in this order: **Obnovit, Tisknout zakázku, Spustit tisk oprav, Spustit tisk** (confirmed via `git show <merge-base>:frontend/src/pages/ExpeditionListArchivePage.tsx`, lines ~192-233). After the split, `ExpeditionJobControlsBar` renders its own `flex gap-2` div containing only `[Tisknout zakázku, Spustit tisk oprav, Spustit tisk]`, and the page now renders `<ExpeditionJobControlsBar />` followed by the `Obnovit` button as a separate sibling in the outer `flex gap-4` container. Because `ExpeditionJobControlsBar` returns a `<>` Fragment (no wrapping element), its children flatten directly into the page's `gap-4` row. The net effect: Obnovit moved from first position (tightly grouped, `gap-2`, immediately after the robot toggle) to last position (after Spustit tisk, now separated by `gap-4` instead of `gap-2`). This contradicts spec FR-1's explicit acceptance criterion ("Visual output ... is pixel-identical: same conditional rendering ..., same disabled/spinner states, same Czech copy" listing the button order as "Obnovit", "Tisknout zakázku", "Spustit tisk oprav", "Spustit tisk") and the stated goal that this is a pure refactor with "no behavior change". Fix by keeping "Obnovit" rendered before `<ExpeditionJobControlsBar />` in the page (restoring first position) or by preserving the original single-group markup/order so the visible button order and spacing match the pre-refactor layout.
