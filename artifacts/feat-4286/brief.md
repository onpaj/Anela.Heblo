## Module
Catalog

## Finding
The pattern:
```csharp
new MarginLevelDto
{
    Percentage = x.Percentage,
    Amount     = x.Amount,
    CostLevel  = x.CostLevel,
    CostTotal  = x.CostTotal
}
```
appears 12 times across two files:

- `GetProductMarginsHandler.cs` (lines 209–269): 4 times in `MapToMarginDto` (M0–M3 averages) + 4 times in the monthly-history select expression
- `GetCatalogDetailHandler.cs` (lines 211–244): 4 times in `GetMarginHistoryFromMargins` (M0–M3)

Every occurrence is identical: all four fields forwarded with no transformation.

## Why it matters
- **Duplication**: Adding, removing, or renaming a field on `MarginLevelDto` requires editing 12 call sites in two files.
- The two handlers are already coupled by the shared contract (`MarginLevelDto` in `Catalog/Contracts/`) — the factory belongs there.

## Suggested fix
Add a static factory method to `MarginLevelDto` (in `backend/src/Anela.Heblo.Application/Features/Catalog/Contracts/MarginLevelDto.cs`):

```csharp
public static MarginLevelDto From(MarginData d) => new()
{
    Percentage = d.Percentage,
    Amount     = d.Amount,
    CostLevel  = d.CostLevel,
    CostTotal  = d.CostTotal
};
```

Replace all 12 inline constructions with `MarginLevelDto.From(m.Value.M0)` etc. No behaviour change.

---
_Filed by daily arch-review routine on 2026-09-23._

---
_Generated by [Claude Code](https://claude.ai/code)_
