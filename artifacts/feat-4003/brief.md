## Module
Journal

## Finding
`SearchJournalEntryDto` (`backend/src/Anela.Heblo.Application/Features/Journal/Contracts/SearchJournalEntryDto.cs`, lines 1–22) is a field-for-field copy of `JournalEntryDto` (`backend/src/Anela.Heblo.Application/Features/Journal/Contracts/JournalEntryDto.cs`, lines 1–28). Both types expose the same eleven properties: `Id`, `Title`, `Content`, `EntryDate`, `CreatedAt`, `ModifiedAt`, `CreatedByUserId`, `CreatedByUsername`, `ModifiedByUserId`, `ModifiedByUsername`, `AssociatedProducts`, `Tags`.

The duplication cascades into the mapper (`backend/src/Anela.Heblo.Application/Features/Journal/Mapping/JournalEntryMapper.cs`, lines 38–66): `ToSearchDto()` is a near-character-for-character copy of `ToDto()` (lines 8–36), differing only in return type. The frontend must import both types from the generated client even though they carry identical data.

## Why it matters
Any field added to `JournalEntryDto` (e.g. a new audit column or tag field) must be duplicated in `SearchJournalEntryDto` and both mapper methods, or the two endpoints drift silently. This is a pure maintenance tax with no present benefit — YAGNI in its clearest form. A reviewer must inspect both types to confirm they are identical rather than assuming they diverge for a reason.

## Suggested fix
1. Remove `SearchJournalEntryDto.cs`.
2. Change `SearchJournalEntriesResponse.Entries` to `List<JournalEntryDto>`.
3. Remove `JournalEntryMapper.ToSearchDto()` and update `SearchJournalEntriesHandler` (line 34) to call `JournalEntryMapper.ToDto()`.
4. Update the frontend import in `JournalList.tsx` (line 22) to remove the `SearchJournalEntryDto` import.

No behavior changes — only dead type aliases are removed.

---
_Filed by daily arch-review routine on 2026-08-31._

---
_Generated by [Claude Code](https://claude.ai/code)_
