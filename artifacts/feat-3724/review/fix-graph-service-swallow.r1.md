# Code Review: Fix silently-swallowed HTTP error in GraphService.GetGroupMembersAsync

## Summary
The implementation replaces the `return new List<UserDto>();` in `GetGroupMembersAsync`'s non-success branch with a thrown `GraphServiceException` wrapping `HttpRequestException(errorContent)`, exactly as specified, and updates/adds tests asserting the new throw behavior for both 403 and 429 status codes. The change is minimal, surgical, and verified against the actual diff (commit `acd7410`) — it matches FR-1 and FR-2 precisely and mirrors the sibling `ODataError` catch block's pattern as required.

## Review Result: PASS

### task: fix-graph-service-swallow
**Status:** PASS

## Overall Notes
Verified against the actual diff (`git show acd7410`), not just the impl summary:

- **FR-1 (throw on non-success HTTP response):** Confirmed. The `!response.IsSuccessStatusCode` branch (GraphService.cs) now throws `new GraphServiceException($"Microsoft Graph returned {(int)response.StatusCode} for group {groupId}.", new HttpRequestException(errorContent))`, replacing the old `return new List<UserDto>();`. The message includes both the numeric status code and `groupId` per the acceptance criteria. `GraphServiceException`'s constructor signature (`string message, Exception innerException`) matches exactly. The pre-existing `_logger.LogError` and `_logger.LogDebug` calls are untouched and still fire before the throw. No other line in `GetGroupMembersAsync`, `SearchUsersAsync`, or `GetAppRoleMembersAsync` was changed — confirmed via the diff, which touches only the 3-line `return` → `throw` replacement in production code.
- **Exception propagation verified:** Traced the surrounding try/catch in `GetGroupMembersAsync`. The new `throw` sits inside a `try` block whose only catches are `MsalException`, `ODataError`, `UnauthorizedAccessException`, and a final `catch (Exception ex) { ...; throw; }` — the last of which rethrows via bare `throw;` rather than swallowing, so `GraphServiceException` propagates to callers unchanged (just logged again). No new swallow was introduced.
- **Architecture adherence:** Confirmed this mirrors the sibling `ODataError` branch a few lines below (same exception type, same "log then throw `GraphServiceException`" shape), as required by the task context and spec background.
- **FR-2 (test update):** Confirmed. `GetGroupMembersAsync_GraphReturnsNonSuccess_ReturnsEmptyList` was renamed to `GetGroupMembersAsync_GraphReturnsNonSuccess_ThrowsGraphServiceException` and now asserts `Assert.ThrowsAsync<GraphServiceException>`, checking the message contains "403" and "group-1" and that `InnerException` is non-null. A second test, `GetGroupMembersAsync_GraphReturnsTooManyRequests_ThrowsGraphServiceException` (429), was added per FR-2's recommended (not required) acceptance criterion, checking "429" and "group-2".
- **Regression scope:** `GetGroupMembersHandlerTests.cs` already contains tests exercising the `catch (GraphServiceException)` path in `GetGroupMembersHandler` (pre-existing, unmodified), consistent with the spec's claim that this path is already tested via the `ODataError` case and requires no handler changes. `IGraphService`'s XML-doc contract for `GetGroupMembersAsync` documents `GraphServiceException` as an expected throw, and the fix now honors it for both failure modes (OData error and raw HTTP non-success).
- **Test execution:** Attempted to run `dotnet test --filter FullyQualifiedName~GraphServiceTests` in the worktree to confirm green, but the run did not complete within the available session time (~15+ minutes, seemingly a slow/first-time build in this sandbox) and was left running in the background; it did not produce a pass/fail result before this review was finalized. This is a sandbox/tooling limitation, not a code concern — the change is a single, well-isolated statement replacement whose correctness was fully verified by direct code reading (diff, exception-propagation trace, and constructor signature check), and the two new/modified test bodies read as correct assertions of the intended behavior (`Assert.ThrowsAsync<GraphServiceException>` with message and `InnerException` checks). If a live pass/fail signal is required before merge, re-run the targeted filter and the full `GraphServiceTests` class in an environment with a warm build cache.
- No changes to `SearchUsersAsync` or `GetAppRoleMembersAsync` were made, consistent with NFR-1 (out of scope).
- Commit `acd7410` contains exactly the two files specified (`GraphService.cs` and `GraphServiceTests.cs`), matching the definition-of-done's "one commit, exactly these two files" requirement.
