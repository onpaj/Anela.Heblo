# Code Review: Unit Test Coverage for DirectManufactureCostProvider

## Summary
The new test file adds 7 xUnit tests covering all three functional requirements (FR-1 concurrency guard, FR-2 unhydrated-cache fallback, FR-3 `FilterByProductCodes` behavior). I traced each test's assertions against the actual production logic in `DirectManufactureCostProvider.cs` line-by-line rather than trusting the implementation summary, and independently ran the test suite. Everything checks out: the tests genuinely exercise the claimed code paths, the concurrency test correctly guarantees the static `RefreshLock` semaphore is released regardless of assertion outcome, and no production code was touched.

## Review Result: PASS

### task: add-direct-manufacture-cost-provider-tests
**Status:** PASS

## Overall Notes

Verification performed:

- **Ran the test suite independently**: `dotnet test test/Anela.Heblo.Tests/Anela.Heblo.Tests.csproj --filter "FullyQualifiedName~DirectManufactureCostProviderTests"` → `Passed! - Failed: 0, Passed: 7, Skipped: 0, Total: 7, Duration: 49 ms`.
- **Confirmed no production code was modified**: `git status --porcelain` / `git diff --stat HEAD` are both clean in the worktree — the test file and all artifact files are already committed (`754b17d test(feat-3499): add DirectManufactureCostProvider unit tests`), and `DirectManufactureCostProvider.cs` is untouched by that commit.
- **FR-1 (`RefreshAsync` concurrency guard) traced against source**: The gate `TaskCompletionSource` is wired to `ICatalogRepository.WaitForCurrentMergeAsync`, which is the first `await` inside `ComputeAllCostsAsync` — called only after `RefreshLock.WaitAsync(0, ct)` has already succeeded in the source (`RefreshAsync`, lines 62-90). This means the first call is genuinely suspended while still holding the lock when the second call is issued, exactly as required. The second call's `WaitAsync(0, ct)` will synchronously fail (lock held), hitting the exact skip-path log message asserted (`"DirectManufactureCostCache refresh already in progress, skipping"`, verbatim match with source line 66). The `finally` block (`gate.TrySetResult(); await firstRefresh;`) runs before any assertion, so the semaphore is provably released before the test can fail on an assertion — this exceeds the spec's minimum bar (spec only required release "before the test method returns"; here it's released even before assertions run). Assertions on `Times.Once` for `WaitForCurrentMergeAsync`/`GetAllAsync`/`SetCachedDataAsync` correctly reflect that only the first (non-skipped) call reaches those calls.
- **FR-2 (unhydrated fallback) traced against source**: `GetCostsAsync` (lines 36-60) checks `cacheData.IsHydrated`; when false it logs the warning (verbatim string matched: `"DirectManufactureCostCache not hydrated yet"`) and returns `new Dictionary<...>()` without ever calling into `_catalogRepository`. The test's `Times.Never` verifications on `GetAllAsync`/`WaitForCurrentMergeAsync` correctly confirm this. A second test confirms non-null filter args (`productCodes`, `dateFrom`, `dateTo`) don't change the outcome, matching the spec's "any combination of args" requirement.
- **FR-3 (`FilterByProductCodes`) traced against source**: The private static method (lines 138-148) returns `allCosts` unchanged when `productCodes == null || !productCodes.Any()`, otherwise filters via `Where(kvp => productCodes.Contains(kvp.Key))`. All four test cases (null → all entries, empty list → all entries, subset with one match + one non-existent → single matching entry, all non-existent → empty) map directly onto these two branches and were verified to pass.
- **Logger-verification helper** (`VerifyLogged`) is structurally identical to the existing precedent in `CatalogMergeSchedulerTests.cs` (same Moq `Log<TState>` overload pattern, `It.Is<It.IsAnyType>` substring match, `Times.AtLeastOnce`), consistent with the architecture review's guidance to avoid brittle exact-string matching.
- Test methods are declared `internal` rather than `public`, matching the exact convention already used in the sibling template `FlatManufactureCostProviderTests.cs` — not a deviation, and confirmed working since the tests were discovered and executed successfully.
- Minor, non-blocking observation: `MonthlyCost` values in `BuildHydratedCacheData()` use a fixed date (`new DateTime(2026, 1, 1)`) rather than a relative date; this is harmless since FR-3 assertions only check dictionary key/count semantics, not date values, and is out of scope per the spec ("no detailed testing of cost values").
