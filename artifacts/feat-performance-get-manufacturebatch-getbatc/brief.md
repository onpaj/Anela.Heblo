## Summary
`GET ManufactureBatch/GetBatchTemplate [productCode]` is averaging 10580ms response time, above the 10000ms threshold.

## App Insights Evidence
- Average response time (24h): **10580ms**
- Threshold: 10000ms
- Endpoint: `GET ManufactureBatch/GetBatchTemplate [productCode]`

## Root Cause Investigation
Possible causes:
- Sequential ingredient lookups for batch template (N+1 pattern, similar to #690 for CalculateBatchBySize)
- External catalog/FlexiBee API calls per ingredient
- Missing eager loading for template with related entities

## Proposed Fix
1. Profile the `GetBatchTemplate` handler
2. Check for N+1 query patterns (similar fix as #690)
3. Add `.Include()` for related entities to avoid lazy-loading loops
4. Cache frequently accessed batch templates

See also: #690 (ManufactureBatch/CalculateBatchBySize slow — same root cause pattern)

---
*Auto-generated by nightly App Insights analysis — 2026-05-12 UTC*