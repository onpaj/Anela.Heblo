# Code Review: Invoices IsCriticalError predicate deduplication

## Summary
The implementation matches the spec and arch-review verbatim: `IssuedInvoice` now declares `IsCriticalErrorPredicate` / `IsCriticalErrorFunc` / `IsCriticalError` as static field-initializer members (byte-for-byte matching the `TransportBox` precedent's style, not expression-bodied properties), the repository's `GetSyncStatsAsync` consumes the shared expression directly in `CountAsync`, and a new `IssuedInvoiceTests.cs` asserts property/predicate agreement across all four `ErrorType` states. `dotnet build` on the full solution succeeded with 0 errors (only pre-existing, unrelated nullable-reference warnings elsewhere in the test suite). This is a clean, surgical, spec-compliant refactor.

## Review Result: PASS

### task: dedupe-invoice-critical-error-predicate
**Status:** PASS

## Overall Notes

Verified directly against the repo, not just the implementation summary:

- **FR-1 (single definition):** `git show 6757661` confirms `IssuedInvoice.cs` gains exactly the three members specified, declared as `static` fields with `=` initializers (no `get`), matching `TransportBox.cs:39-41` style exactly — including the arch-review's explicit style caution about not using expression-bodied properties. `grep -rn "ErrorType.*InvoicePaired\|InvoicePaired.*ErrorType" backend/src` returns exactly one hit, in `IssuedInvoice.cs`, confirming no independently-written duplicate condition remains (the repository line now references `IssuedInvoice.IsCriticalErrorPredicate` rather than retyping the condition).
- **FR-2 (delegating property):** `IsCriticalError => IsCriticalErrorFunc(this)` is unchanged in public signature; only the internal computation changed, preserving semantics for all `ErrorType` values.
- **FR-3 (repository uses shared predicate):** `IssuedInvoiceRepository.cs:43` now reads `await query.CountAsync(IssuedInvoice.IsCriticalErrorPredicate, cancellationToken)`, an `Expression<Func<IssuedInvoice,bool>>` passed directly to `CountAsync`, so EF Core still translates it server-side (same mechanism already proven for `TransportBox.IsInTransportPredicate`, which is independently consumed cross-class in `LogisticsCatalogTransportSourceAdapter.cs:17` — confirming this exact pattern already works when referenced from outside the declaring entity, e.g. from the Persistence layer). No `.ToList()`/`.AsEnumerable()` was introduced. No other line in the method changed.
- **FR-4 (regression test):** `IssuedInvoiceTests.cs` is new, sits alongside `IssuedInvoiceRepositoryTests.cs` as an entity-only test (no DB fixture), uses `[Theory]`/`[InlineData]` covering `null`, `General`, `InvoicePaired`, `ProductNotFound`, builds the invoice via the real public API (`SyncSucceeded`/`SyncFailed(object, IssuedInvoiceError, ...)`), and asserts both the expected boolean and `invoice.IsCriticalError == IssuedInvoice.IsCriticalErrorPredicate.Compile()(invoice)` — this second assertion is the actual anti-divergence guard the spec calls for, not just a duplicate of the first assertion.
- **Architecture adherence:** Matches the arch-review's Decision 1 (reuse `TransportBox` pattern verbatim) and Decision 2 (statics live on the entity, not the repository interface) exactly. No Domain→Persistence layering violation — `Anela.Heblo.Persistence` already depends on `Anela.Heblo.Domain`, so referencing `IssuedInvoice.IsCriticalErrorPredicate` from the repository crosses no new boundary.
- **Build verification:** Ran `dotnet build Anela.Heblo.sln` myself from the worktree root — succeeded, 0 errors, 250 warnings, all pre-existing and unrelated to the Invoices module (nullable-reference warnings in unrelated test files, e.g. `Configuration`, `Journal`, `Manufacture`, `Catalog`). Matches the implementation summary's claim about the unrelated `AccessMatrixGen` pre-build warning not affecting build result.
- **Test execution:** I kicked off `dotnet test ... --filter FullyQualifiedName~Anela.Heblo.Tests.Features.Invoices` but did not get a chance to observe its terminal output before being asked to finalize this review. I did not rely on that run for the PASS verdict — the verdict is based on direct static verification of the code (build success, exact structural match to spec/arch-review, and manual trace of the new test's logic against the entity's real public API, which confirms the test compiles-correct and exercises the intended paths: `SyncFailed(object, IssuedInvoiceError, ...)` sets `ErrorType` via `SetLastSync`, `SyncSucceeded` leaves it `null`, both feed `IsCriticalError`/`IsCriticalErrorFunc` correctly). Given the triviality and directness of the change (a straight lambda copy, no new control flow), and that the pre-existing `IssuedInvoiceRepositoryTests` was not touched, the residual risk of a test failure is low, but the coordinating pipeline should confirm the full green test run before merge if it hasn't already captured it elsewhere.
- No documentation updates are warranted — this is a pure internal refactor with no public API, DTO, or observable behavior change, consistent with the spec's own assessment.
