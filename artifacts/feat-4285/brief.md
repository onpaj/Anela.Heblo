## Module
Catalog

## Finding
`CatalogDataRefreshService` (`backend/src/Anela.Heblo.Application/Features/Catalog/Infrastructure/CatalogDataRefreshService.cs`, lines 46–90) requires 22 injected dependencies:

- 11 external API clients (`ICatalogSalesClient`, `ICatalogSetPartsClient`, `IEshopStockClient`, `IConsumedMaterialsClient`, `IPurchaseHistoryClient`, `IErpStockClient`, `ILotsClient`, `IProductPriceEshopClient`, `IProductPriceErpClient`, `IProductEshopUrlClient`, `ICatalogAttributesClient`)
- 3 cross-module sources (`ICatalogTransportSource`, `ICatalogPurchaseSource`, `ICatalogManufactureSource`)
- 2 repositories (`IStockTakingRepository`, `IManufactureDifficultyRepository`)
- Resilience + time + options + cache store + logger

Each `Refresh*` method is an independent fetch-then-cache operation; the class is a collection of ~14 unrelated responsibilities jammed into one type.

## Why it matters
- **SRP violation**: The class has as many reasons to change as it has data sources. Adding a new source, swapping a client, or changing a cache strategy for one source forces changes to this single class.
- **Testability**: Every unit test that covers one `Refresh*` method must satisfy all 22 constructor parameters.
- **Interface Segregation**: Any collaborator that schedules a refresh task depends on the full 22-parameter graph even when it only calls one method.

## Suggested fix
Split into 3–4 cohesive classes grouped by source family:

- **`CatalogHistoryRefreshService`** – sales, set-parts, consumed, purchase history, manufacture history (5 clients)
- **`CatalogStockRefreshService`** – ERP stock, eshop stock, transport source, quarantine/reserve (3 clients + 1 cross-module source)
- **`CatalogMetaRefreshService`** – attributes, lots, prices (eshop + ERP), eshop URLs (5 clients)
- **`CatalogReferenceRefreshService`** – stock taking, manufacture difficulty, planned/ordered quantities (2 repos + 2 sources)

Each new class inherits the same `CatalogCacheStore` + `ICatalogResilienceService` dependencies. No behavioural change is required; the existing `CatalogModule.RegisterBackgroundRefreshTasks` method wires up concrete methods by interface, so it routes to whichever class owns each task.

---
_Filed by daily arch-review routine on 2026-09-23._

---
_Generated by [Claude Code](https://claude.ai/code)_
