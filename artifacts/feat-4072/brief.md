## Module
Logistics

## Finding
`SetGiftSettingHandler` (`backend/src/Anela.Heblo.Application/Features/Logistics/UseCases/GiftSettings/UseCases/SetGiftSetting/SetGiftSettingHandler.cs`, lines 33–58) manually re-validates rules that `SetGiftSettingValidator` already enforces:

| Rule | In Validator | In Handler |
|------|-------------|------------|
| `ThresholdCzk > 0` when enabled | ✓ line 13 | ✓ line 37 |
| `Text` not empty when enabled | ✓ line 14 | ✓ line 42 |
| `Text.Length ≤ 50` | ✓ line 9 | ✓ line 52 |

`GiftSettingsModule` (`GiftSettingsModule.cs`, line 17) registers `ValidationBehavior<SetGiftSettingCommand, SetGiftSettingResponse>` as a MediatR pipeline behavior that runs `SetGiftSettingValidator` before `SetGiftSettingHandler.Handle()` is ever called. The handler's `if`-blocks therefore execute only if the pipeline behavior is missing or bypassed — which should never happen — so they provide false safety while adding noise and divergence risk (the two rule sets can drift).

## Why it matters
Duplicated validation is a maintenance burden: a future rule change (e.g. increasing the max length from 50 to 100) requires editing both files. The handler also returns `ErrorCodes.ValidationError` with English `message` param strings that bypass the structured `Params` pattern used by the pipeline's `ValidationException` mapping, producing different error shapes depending on which guard triggers.

## Suggested fix
Remove lines 33–58 from `SetGiftSettingHandler` and trust the `ValidationBehavior` pipeline. Keep the `MaxTextLength` constant in the validator if needed. The handler body then reduces to: resolve user → build entity → save → return success.

---
_Filed by daily arch-review routine on 2026-09-05._

---
_Generated by [Claude Code](https://claude.ai/code)_
