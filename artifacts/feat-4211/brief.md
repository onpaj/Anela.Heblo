## Module
Manufacture

## Finding
`getManufacturingSeverityColorClass` is exported from `frontend/src/api/hooks/useManufacturingStockAnalysis.ts` (lines 126–143) but has **zero consumers** in the codebase (verified with a full-repo grep). The function returns hardcoded light-only Tailwind color strings:

```ts
case ManufacturingStockSeverity.Critical:
  return "text-red-600 bg-red-50";
case ManufacturingStockSeverity.Major:
  return "text-orange-600 bg-orange-50";
// ... all five cases, no dark: variants
```

The component that would logically use it (`ManufacturingStockAnalysis.tsx`) instead has its own inline dark-mode-aware classes (e.g. `text-red-600 dark:text-red-400`). The exported function is therefore orphaned dead code.

## Why it matters
1. **YAGNI / dead code**: Exported symbols that are never imported add surface area with no benefit. Any future consumer that discovers the function will get broken dark-mode styling without any compile-time warning.
2. **ADR-006 violation if reactivated**: ADR-006 requires every frontend color class to include a `dark:` sibling. The exported function has none, so copying it into a new component would silently break dark mode.

## Suggested fix
Delete the function and its related `getManufacturingSeverityDisplayText` sibling if that is also unused (verify with grep). If any future use case needs severity-to-color mapping it should be added with proper `dark:` variants at that point.

---
_Filed by daily arch-review routine on 2026-09-17._

---
_Generated by [Claude Code](https://claude.ai/code)_
