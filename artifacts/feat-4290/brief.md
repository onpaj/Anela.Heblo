## Module
Marketing / MarketingPerformance

## Finding
`IssuedInvoiceMonthlyRevenueSource` sits in `backend/src/Anela.Heblo.Persistence/Marketing/IssuedInvoiceMonthlyRevenueSource.cs` (a Marketing-scoped persistence folder) and is registered in `MarketingPerformanceModule.cs` (line 22: `services.AddScoped<IMonthlyRevenueSource, IssuedInvoiceMonthlyRevenueSource>()`). Its implementation directly queries `_context.IssuedInvoices` (lines 27–34), a `DbSet<IssuedInvoice>` where `IssuedInvoice` lives in `Anela.Heblo.Domain.Features.Invoices` — a different module.

## Why it matters
`development_guidelines.md` forbids "Direct access to another module's entities" and specifies the cross-module communication pattern:
> **Provider (B) implements the contract via an adapter.** The adapter lives in module B's `Infrastructure/`.
> **Provider (B) registers the DI binding.** `{Module}.cs` of the provider registers the binding; the consumer module never touches this registration.

Here the consumer (`MarketingPerformance`) both owns the adapter and registers it. This means `MarketingPerformance` takes a hard compile-time dependency on the `Invoices` domain entity. If the `IssuedInvoice` entity is renamed, restructured, or moved, `IssuedInvoiceMonthlyRevenueSource` breaks — but the break belongs to the wrong module and is not caught by any module-boundary test. It also makes it impossible to extract `Invoices` into a separate service later without also touching `MarketingPerformance`.

## Suggested fix
1. Move `IssuedInvoiceMonthlyRevenueSource.cs` to `backend/src/Anela.Heblo.Persistence/Invoices/IssuedInvoiceMonthlyRevenueSource.cs` (or `Application/Features/Invoices/Infrastructure/`).
2. Change its namespace from `Anela.Heblo.Persistence.Marketing` to `Anela.Heblo.Persistence.Invoices`.
3. Move the DI registration from `MarketingPerformanceModule.cs` to `InvoicesModule.cs`:
   ```csharp
   services.AddScoped<IMonthlyRevenueSource, IssuedInvoiceMonthlyRevenueSource>();
   ```
4. Remove the `using Anela.Heblo.Persistence.Marketing;` import from `MarketingPerformanceModule.cs`.

This matches the `ILeafletKnowledgeSource` → `KnowledgeBaseLeafletSourceAdapter` pattern documented in the guidelines.

---
_Filed by daily arch-review routine on 2026-09-24._

---
_Generated by [Claude Code](https://claude.ai/code)_