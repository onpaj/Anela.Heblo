Plan saved to `docs/plans/2026-06-03-persist-scheduled-dqt-runs.md`.

**Summary:** 4 tasks (3 TDD cycles + 1 validation), one per scheduled DQT job plus a final build/format/test gate. Each job task creates the first-ever test file for its class (none existed) and asserts the `AddAsync → SaveChangesAsync → RunAsync` call order, the disabled-job short-circuit, and `cancellationToken` propagation — then applies the one-line fix between `AddAsync` and `RunAsync`. All file paths, method signatures, enum values, and mock-setup patterns are verified against the actual source (`InvoiceDqtJob.cs:48`, `BaseRepository.cs:57,97`, `IRecurringJobStatusChecker.cs:5`, etc.). Code blocks contain complete, paste-ready content with no placeholders. Spec coverage table maps every FR/NFR to a specific step; self-review confirmed type consistency across all three test files.