Plan saved to `docs/superpowers/plans/2026-06-08-honest-dependency-shoptetapi-expedition-list-source.md`.

**Summary:** Five-task, behavior-preserving refactor plan that (1) captures a green baseline, (2) changes the `ShoptetApiExpeditionListSource` constructor parameter from `IEshopOrderClient` to the concrete `ShoptetOrderClient` and removes both the downcast and the now-stale comment, (3) runs `dotnet format` to let the toolchain handle the unused `using`, (4) re-runs targeted and full test suites to confirm nothing regressed, and (5) commits with a message that explains the why and references the prior `ShoptetApiPackingOrderClient` precedent. DI registration and existing tests need no edits — both were verified to already supply a concrete `ShoptetOrderClient`, matching the architecture review's findings.