# Code Review: add-sync-stats-sql-shape-test

## Summary
The implementation adds exactly the file the task context specified — `IssuedInvoiceRepositoryGetSyncStatsSqlShapeTests.cs` — matching the `PhotobankRepositoryGetTagsSqlShapeTests` convention closely (same fixture, same private `DbCommandInterceptor`, same minimal-schema approach). I independently rebuilt the test project and ran the new test class against a real Postgres/Testcontainers instance: both facts pass (2/2, 96ms), confirming the NFR-1 "1 round trip" claim is now actually enforced by a relational-provider test rather than left unverified by the InMemory suite.

## Review Result: PASS

### task: add-sync-stats-sql-shape-test
**Status:** PASS

## Overall Notes
- **Diff matches summary.** `git show 7f383df` is a single new file, 129 lines, no unrelated changes — consistent with the impl summary and the task-context's prescribed content verbatim.
- **Architecture fit confirmed.** Compared line-by-line against `PhotobankRepositoryGetTagsSqlShapeTests.cs`: same `[Collection("PostgresIntegration")]` / `[Trait("Category", "Integration")]` attributes, same `PostgresSharedContainerFixture` + `IAsyncLifetime` lifecycle, same non-shared private `CapturingCommandInterceptor` (intentionally duplicated per-class, matching the arch review's explicit guidance not to introduce a shared helper), same "minimal schema, only columns the query touches" approach (`Id`, `InvoiceDate`, `IsSynced`, `ErrorType`, `LastSyncTime` — omitting `DueDate`, `Price`, etc., which is safe since the `GroupBy`/aggregate projection never selects those columns).
- **Correctness of seeded data verified by hand.** Checked `IssuedInvoiceErrorType` enum (`General=0`, `InvoicePaired=1`, `ProductNotFound=2`) against the seed rows and assertions: `TotalInvoices=4` (INV-OLD correctly excluded as out-of-range), `SyncedInvoices=1`, `UnsyncedInvoices=3`, `InvoicesWithErrors=2` (INV-ERROR + INV-PAIRED both have non-null `ErrorType`), `CriticalErrors=1` (only INV-ERROR, since INV-PAIRED's `ErrorType=1` is `InvoicePaired` and correctly excluded), `LastSyncTime = 2026-01-13 10:00:00` (max among INV-SYNCED/INV-ERROR/INV-PAIRED, all in-range). All assertions are arithmetically and semantically correct against the current `GetSyncStatsAsync` implementation.
- **Independent re-run.** `dotnet build test/Anela.Heblo.Tests/Anela.Heblo.Tests.csproj` succeeded with 0 errors (only pre-existing, unrelated nullable-reference warnings elsewhere in the suite). `dotnet test ... --filter "FullyQualifiedName~IssuedInvoiceRepositoryGetSyncStatsSqlShapeTests" --no-build` passed 2/2 against a real Docker-backed Postgres container, corroborating the impl summary's reported result.
- **Working tree is clean** (`git status --short` shows only an unrelated `artifacts/feat-3564/state.json` pipeline-bookkeeping change) — the task context's Step 3 non-destructive regression-proving procedure (temporarily reintroducing the 5-query implementation, confirming the test fails with 5 commands, then restoring) left no residue, consistent with the impl summary's claim that this was done and reverted cleanly. I did not re-run that specific revert/restore cycle myself (not necessary to confirm correctness of the committed artifact — the working tree state is proof no accidental leftover occurred).
- Minor, non-blocking: `dotnet format --verify-no-changes` could not be re-run standalone from this review session (no `.sln` found at `backend/` root for the bare invocation used), but `dotnet build` succeeded cleanly and the new file's formatting is structurally identical to the already-formatted `PhotobankRepositoryGetTagsSqlShapeTests.cs`, so this is not a concern.
