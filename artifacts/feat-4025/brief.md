## Module
PackingMaterials

## Finding
`IConsumptionCalculationService` exposes `HasDayAlreadyBeenProcessedAsync` at the interface level, but no external consumer ever calls it through the interface.

File: `backend/src/Anela.Heblo.Application/Features/PackingMaterials/Services/IConsumptionCalculationService.cs:9`

The only callers are:
- `ConsumptionCalculationService.ProcessDailyConsumptionAsync` (self-call on the concrete class, not via the interface) — `Services/ConsumptionCalculationService.cs:28`
- `ConsumptionCalculationServiceTests` test `HasDayAlreadyBeenProcessedAsync_ShouldReturnCorrectValue` (calls it directly on the concrete service instance, not through the interface) — `test/.../ConsumptionCalculationServiceTests.cs:245`

`ProcessDailyConsumptionHandlerTests` mocks `IConsumptionCalculationService` (`ProcessDailyConsumptionHandlerTests.cs:20`) but never stubs, uses, or needs `HasDayAlreadyBeenProcessedAsync`.

## Why it matters
This violates the Interface Segregation Principle: the interface surface area is wider than what any consumer needs. Any mock of `IConsumptionCalculationService` must carry this method even though no handler or background job depends on it. The method is an idempotency implementation detail of `ConsumptionCalculationService`; promoting it to the public interface implies external callers should use it independently, which is misleading and couples any future alternative implementation to this internal concern.

## Suggested fix
Remove `HasDayAlreadyBeenProcessedAsync` from `IConsumptionCalculationService`. Make the method `private` inside `ConsumptionCalculationService`. The test can be refactored to verify the same behaviour indirectly through `ProcessDailyConsumptionAsync`: call it twice for the same date and assert `WasRun: false` on the second call.

---
_Filed by daily arch-review routine on 2026-09-01._

---
_Generated by [Claude Code](https://claude.ai/code)_
