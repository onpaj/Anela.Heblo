## Review Result: CLEAN

### Blocking (correctness)
- None

### Advisory (cleanup)
- `backend/src/Anela.Heblo.Persistence/Smartsupp/SmartsuppRepository.cs:11,15,19` — `_logger`/`ILogger<SmartsuppRepository> logger` is now dead: the only call site (`LogWarning` inside `TryFetchAndStageContactAsync`) moved to `SmartsuppContactEnricher` along with the REST fetch, but the field, constructor parameter, and `using Microsoft.Extensions.Logging;` were left in place with no remaining read/write beyond the assignment. Consider dropping the parameter and field (and updating the two test call sites that pass `NullLogger<SmartsuppRepository>.Instance`) unless it's being kept deliberately for near-term future use.
- `backend/src/Anela.Heblo.Application/Features/Smartsupp/Infrastructure/ISmartsuppContactEnricher.cs:49` / `backend/src/Anela.Heblo.Persistence/Smartsupp/SmartsuppRepository.cs:86-91` — the "contact already known locally" path now costs two DB round-trips instead of one: `SmartsuppContactEnricher.EnrichContactAsync` calls `ContactExistsAsync` (an `AnyAsync`) purely to decide whether to skip REST, and then `SmartsuppRepository.UpsertConversationAsync` immediately re-queries the same `SmartsuppContacts` row by the same id (`FirstOrDefaultAsync`) to hydrate `ContactName`/`ContactEmail`. Pre-refactor this was a single lookup. This matches the interface shape mandated by `spec.r1.md`'s "API/Interface Design" section, so it's a deliberate design tradeoff rather than an implementation slip — flagging only as a minor, non-blocking efficiency note in case a follow-up wants to collapse it (e.g. have `EnrichContactAsync` return the resolved contact so the repository doesn't need to re-fetch).

## Notes
Reviewed the full diff against `merge-base(origin/main, HEAD)` (`eb877423f...HEAD`, backend-only — no frontend changes, consistent with the spec's "no public API changes"). Verified against `spec.r1.md` FR-1 through FR-5 and NFR-1 through NFR-3:

- **FR-1** — confirmed no `ISmartsuppApiClient` reference remains anywhere under `Anela.Heblo.Persistence`; `SmartsuppRepository`'s constructor is now `(ApplicationDbContext db, ILogger<SmartsuppRepository> logger)` only.
- **FR-2** — `UpsertConversationAsync` still looks up the locally-known contact and hydrates `ContactName`/`ContactEmail` via `??=`, unchanged; the `conversation.ContactId = null` wipe-on-miss branch and the REST call are both gone from the repository, with no SQL changes (upsert statement text is byte-identical to before).
- **FR-3** — `SmartsuppContactEnricher` (`Anela.Heblo.Application/Features/Smartsupp/Infrastructure/`) depends only on `ISmartsuppApiClient`, `ISmartsuppRepository`, `ILogger<SmartsuppContactEnricher>`; implements the exact existence-check → REST-fetch → map → stage → hydrate → fail-open-wipe flow specified, including the required DB-row existence check (`ContactExistsAsync`, not a DTO-field check) and the broad `catch (Exception ex)` fail-open contract carried over verbatim, with the `DateTimeKind.Utc` mapping comment preserved.
- **FR-4** — traced every `UpsertConversationAsync` call site: all seven reactions (`ConversationOpenedReaction`, `ConversationRatedReaction`, `ConversationClosedReaction`, `ConversationClosedByContactReaction`, `ConversationAgentAssignedReaction`, `ConversationAgentUnassignedReaction`, `ConversationReplyReactionBase`) now call `EnrichContactAsync` immediately before the upsert; `ConversationReplyReactionBase`'s message-only branch is correctly left untouched. `RefreshOrphanContactsHandler` now injects the enricher and calls it after `local.ContactId = remote.ContactId` and before `UpsertConversationAsync`, preserving the existing `Updated`/`Failed`/`SkippedNoContactId` counters and the handler's own `SaveChangesAsync` call. `ProcessWebhookEventHandler`'s single post-reaction `SaveChangesAsync` is unchanged (file untouched by this diff), so the transactional grouping described in the acceptance criteria still holds. `SmartsuppModule.cs` registers `ISmartsuppContactEnricher` alongside the other Smartsupp scoped services, so DI resolution is covered for all affected reaction types plus the handler.
- **FR-5** — `SmartsuppRepositoryUnknownContactFetchTests` had its four REST-behavior tests removed and confirmed present (renamed/ported) in the new `SmartsuppContactEnricherTests`, covering fetch-on-miss, wipe-on-null, wipe-on-throw, and skip-when-known-locally, including a `MockBehavior.Strict` REST client to prove no call happens on the known-locally path. `ListOrphanContactConversationIdsAsync`'s test stayed in place, updated only to drop the `apiClient` constructor argument. `SmartsuppRepositoryUpdatedAtGuardTests`'s shared factory and `SmartsuppRepositoryUpsertIntegrationTests` both dropped the `apiClient` argument with no other changes. `ConversationReactionsTests` adds a pass-through `ISmartsuppContactEnricher` mock and threads it into every reaction constructor call; `SmartsuppWebhookControllerTests`'s `NoOpSmartsuppRepository` implements the new `ContactExistsAsync` interface member.
- **NFR-3** — checked for other `ISmartsuppRepository` implementations that would need to add `ContactExistsAsync`; only the real `SmartsuppRepository` and the test `NoOpSmartsuppRepository` implement the interface, both updated.

Verification performed: `dotnet build` on `Anela.Heblo.API.csproj` succeeds with 0 errors and no new warnings attributable to this change (all 155 warnings are pre-existing, unrelated to Smartsupp). Grepped for lingering references to the removed `SmartsuppRepository.MapContactDataToEntity` / `TryFetchAndStageContactAsync` / old 3-arg constructor — none found; the one caller that needed repointing (`SmartsuppContactMappingTests`) was updated to `SmartsuppContactEnricher.MapContactDataToEntity`, which is `internal` and reachable via the existing `InternalsVisibleTo("Anela.Heblo.Tests")` on `Anela.Heblo.Application`. A full `dotnet test --filter FullyQualifiedName~Smartsupp` run in this sandbox did not complete within available time (this sandbox lacks a Docker daemon, and the Postgres-Testcontainers-backed integration tests in this filter are slow/blocking here) — the prior task-level review (`artifacts/feat-3878/review/remove-rest-dependency-from-smartsupp-repository.r1.md`) already ran and documented this exact filter passing except for the same pre-existing Docker-dependent integration tests, which is consistent with what's expected in this environment.
