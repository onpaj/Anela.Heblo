## Module
DataQuality

## Finding
`ProductPairingDqtComparer` in `backend/src/Anela.Heblo.Application/Features/DataQuality/Services/ProductPairingDqtComparer.cs` (lines 3–4) imports types owned by the Catalog module:

```csharp
using Anela.Heblo.Domain.Features.Catalog;
using Anela.Heblo.Domain.Features.Catalog.Stock;
```

The class injects and operates on `IEshopStockClient`, `IErpStockClient` (defined in `Anela.Heblo.Domain.Features.Catalog.Stock`), and uses `EshopStock`, `ErpStock`, and `ProductType` (from `Anela.Heblo.Domain.Features.Catalog`). These are Catalog-owned domain types used directly in a DataQuality service.

## Why it matters
This is a direct cross-module dependency violation. Per `development_guidelines.md`, feature modules must not reference each other's domain types; communication happens through consumer-owned contracts and provider-side adapters (the `ILeafletKnowledgeSource` pattern). Any change to the Catalog domain types breaks DataQuality at compile time, and DataQuality cannot be tested in isolation.

## Suggested fix
1. Add DataQuality-owned contract interfaces to `backend/src/Anela.Heblo.Application/Features/DataQuality/Contracts/` (e.g., `IDqtEshopStockSource`, `IDqtErpStockSource`) with DataQuality-owned snapshot types (`DqtEshopStockItem`, `DqtErpStockItem`) containing only what DataQuality needs.
2. Add adapters in `backend/src/Anela.Heblo.Application/Features/Catalog/Infrastructure/` implementing these contracts by delegating to the existing Catalog clients.
3. Register the adapters in `CatalogModule.cs`.
4. Remove the Catalog `using` directives from `ProductPairingDqtComparer` and inject the new contracts.

---
_Filed by daily arch-review routine on 2026-08-28._

---
_Generated by [Claude Code](https://claude.ai/code)_
