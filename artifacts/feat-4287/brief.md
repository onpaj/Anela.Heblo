## Module
Catalog

## Finding
`GetProductMarginsHandler.ApplySorting` (`backend/src/Anela.Heblo.Application/Features/Catalog/UseCases/GetProductMargins/GetProductMarginsHandler.cs`, lines 129–186) contains a `switch` expression with 16 arms. Every arm is structurally identical:

```csharp
"fieldname" => sortDescending
    ? products.OrderByDescending(x => x.SomeField)
    : products.OrderBy(x => x.SomeField),
```

Only the selector lambda differs. The `sortDescending` ternary is duplicated 17 times (16 arms + the default).

## Why it matters
- Adding a new sortable column currently requires two lines per arm instead of one.
- The ternary noise makes it hard to scan the list of supported sort fields.
- Any fix to the sort-direction logic (e.g. null-safe comparison) must be applied to all 17 sites.

## Suggested fix
Extract a private static helper:

```csharp
private static IEnumerable<CatalogAggregate> SortBy<TKey>(
    IEnumerable<CatalogAggregate> items,
    Func<CatalogAggregate, TKey> key,
    bool desc)
    => desc ? items.OrderByDescending(key) : items.OrderBy(key);
```

Each `switch` arm then becomes a single line:

```csharp
"productcode" => SortBy(products, x => x.ProductCode, sortDescending),
"m0amount"    => SortBy(products, x => x.Margins.Averages.M0.Amount, sortDescending),
// …
```

No behaviour change. The `default` arm and the null-guard before the switch collapse the same way.

---
_Filed by daily arch-review routine on 2026-09-23._

---
_Generated by [Claude Code](https://claude.ai/code)_
