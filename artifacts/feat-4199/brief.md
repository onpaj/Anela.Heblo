## Module
Purchase

## Finding
`PurchaseStockAnalysisController.cs:26` returns `BadRequest(ModelState)` when model validation fails, while every other Purchase controller uses `ErrorResponseHelper.CreateValidationError<T>()`:

```csharp
// PurchaseStockAnalysisController.cs (current — wrong)
return BadRequest(ModelState);

// PurchaseOrdersController.cs (correct pattern)
return BadRequest(ErrorResponseHelper.CreateValidationError<CreatePurchaseOrderResponse>());
```

File: `backend/src/Anela.Heblo.API/Controllers/PurchaseStockAnalysisController.cs`, line 26

## Why it matters
The inconsistency breaks the project's unified API error contract. When the stock analysis endpoint receives an invalid request (e.g. `PageSize` out of the `[Range(1, 100)]` bound), it returns ASP.NET Core's raw `ModelState` serialization instead of the project's `BaseResponse`-derived error envelope. Frontend error handlers that key on `BaseResponse.Success` / `BaseResponse.ErrorCode` will not see these errors in the expected format, causing silent failures or incorrect UX in the stock analysis view.

## Suggested fix
Replace line 26 with the standardized helper — a one-line change consistent with every other action in the Purchase area:

```csharp
if (!ModelState.IsValid)
{
    return BadRequest(ErrorResponseHelper.CreateValidationError<GetPurchaseStockAnalysisResponse>());
}
```

---
_Filed by daily arch-review routine on 2026-09-15._

---
_Generated by [Claude Code](https://claude.ai/code)_
