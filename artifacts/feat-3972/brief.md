telemetry-signal: exception:JsonReaderException@ClaudeMeetingTaskExtractor.ExtractAsync

## Signal

`ClaudeMeetingTaskExtractor.ExtractAsync`
(`backend/src/Anela.Heblo.Application/Features/MeetingTasks/Services/ClaudeMeetingTaskExtractor.cs:76`)
catches `JsonException` when parsing the LLM's response and silently returns
an **empty** `MeetingExtractionResult` — the meeting is imported with zero
tasks and zero participants, with only a `LogError` and no user-visible
signal.

## Data — P7D window (2026-08-21 → 2026-08-28)

3 occurrences, all `System.Text.Json.JsonReaderException`, all a JSON syntax
error mid-way through the `tasks` array (not at the end of the response, so
not consistent with output truncation at the 8192 `MaxOutputTokens` cap):

| Timestamp (UTC) | Error | Path |
|---|---|---|
| 2026-08-25T07:51:20Z | `'v' is invalid after a value` | `$.tasks[5]`, line 39, byte 78 |
| 2026-08-25T08:51:27Z | `'0xE2' is invalid after a value` | `$.tasks[7]`, line 54, byte 89 |
| 2026-08-26T06:47:00Z | `'v' is invalid after a value` | `$.tasks[0]`, line 4, byte 44 |

Only ~13 background-job calls to `api.anthropic.com` in this operation this
week (no `operation_Name` — Hangfire-triggered), so this is roughly a **23%
failure rate** for the extraction job over the window, not an isolated blip.
The `0xE2` byte is the lead byte of a multi-byte UTF-8 sequence (e.g. an
em-dash, curly quote, or accented Czech character) — the varying error
positions and characters point at the model occasionally emitting an
unescaped raw character inside a JSON string value (likely a Czech
name/quote in `title`/`description`/`assignee`), not a systematic
truncation issue.

## Why this is worth a look

- App-owned code, no existing telemetry issue for this fingerprint.
- The prompt (`BasePrompt` in the same file) asks the model to "return ONLY a
  JSON object" via freeform text + `JsonSerializer.Deserialize`, with no
  schema-constrained/tool-use output — nothing prevents the model from
  emitting invalid JSON, and when it does, the current handling silently
  drops the entire meeting's tasks rather than degrading partially.
- The exception message itself isn't currently enough to see the malformed
  content — the raw response text isn't logged on parse failure, so root
  cause can't be nailed further from telemetry alone.

## Next step

Log the raw `response.Text` (or at least a window around the reported
`BytePositionInLine`) at the `catch (JsonException)` site
(`ClaudeMeetingTaskExtractor.cs:76-80`) so the next occurrence's actual
malformed payload is visible; longer-term, consider Anthropic
structured-output/tool-use with a JSON schema instead of prompt-only JSON to
eliminate this failure mode, since the current fallback silently discards a
real meeting's action items with no visible signal to the user who ran the
extraction.

---
_Generated by [Claude Code](https://claude.ai/code)_
