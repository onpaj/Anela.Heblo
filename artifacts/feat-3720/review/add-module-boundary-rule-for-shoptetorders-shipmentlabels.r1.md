# Code Review: add-module-boundary-rule-for-shoptetorders-shipmentlabels

## Summary
The implementation adds exactly the `HashSet<string>` allowlist field and `ModuleBoundaryRule` entry prescribed by the task spec, in the exact location specified, with matching text. Direct inspection of the full diff, the resulting `ModuleBoundariesTests.cs`, and the rest of the feature's code confirms the rule is correctly wired and that `ShoptetOrders` (including `CompleteDeliveredOrdersJob`) has no remaining reference to `ShipmentLabels`, closing out the feature as intended.

## Review Result: PASS

### task: add-module-boundary-rule-for-shoptetorders-shipmentlabels
**Status:** PASS

## Docs to Update
(None)

## Overall Notes
- Verified via `git show 7f8e986` (the task's commit): the diff touches only `backend/test/Anela.Heblo.Tests/Architecture/ModuleBoundariesTests.cs`, adding 15 lines — the `ShoptetOrdersShipmentLabelsAllowlist` field (empty `HashSet<string>` with justification comment) right after `PackagingShoptetOrdersAllowlist`, and a new `ModuleBoundaryRule("ShoptetOrders -> ShipmentLabels", ...)` as the last entry in `Rules()`. Both insertions match the task-context's prescribed before/after snippets verbatim.
- The rule shape (`InspectedNamespacePrefix: "Anela.Heblo.Application.Features.ShoptetOrders"`, forbidding `"Anela.Heblo.Application.Features.ShipmentLabels"`, empty allowlist) matches spec FR-6's acceptance criteria exactly.
- Confirmed via `grep -rn "ShipmentLabels" backend/src/Anela.Heblo.Application/Features/ShoptetOrders/` that no file under `ShoptetOrders` (including `Contracts/IShipmentDeliveryChecker.cs`) references anything in the `ShipmentLabels` namespace — zero matches.
- Read `CompleteDeliveredOrdersJob.cs` in full: it depends only on `IShipmentDeliveryChecker` (field, ctor parameter), imports `Anela.Heblo.Application.Features.ShoptetOrders.Contracts`, and the call site (`_shipmentClient.HasDeliveredShipmentAsync(order.Code, cancellationToken)`) and all other logic/metadata are unchanged from the prior task's swap.
- Cross-checked the whole chain from the earlier three tasks in this feature: `Contracts/IShipmentDeliveryChecker.cs` (one method, matches spec), `ShipmentLabels/Infrastructure/ShipmentLabelsShipmentDeliveryCheckerAdapter.cs` (`internal sealed`, pure delegation to `IShipmentClient`), and `ShipmentLabelsModule.cs` (`services.AddTransient<IShipmentDeliveryChecker, ShipmentLabelsShipmentDeliveryCheckerAdapter>()` with the same justification-comment convention as `CarrierCoolingModule.cs`) are all present, correct, and consistent with the sibling `IPackingCarrierCoolingSource`/`IPackingProductSource` pattern this spec deliberately mirrors.
- `CompleteDeliveredOrdersJobTests.cs` uses `Mock<IShipmentDeliveryChecker>` and imports `ShoptetOrders.Contracts` — no leftover `IShipmentClient`/`ShipmentLabels` references.
- Read `EnumerateReferencedTypes`/`IsForbidden` in `ModuleBoundariesTests.cs`: the reflection walk inspects fields, properties, constructor parameters, and method parameters/return types (declared-only, non-public included), which is sufficient to catch a field of type `IShipmentClient` or a constructor parameter of that type if one were reintroduced into `ShoptetOrders` — the rule is a real regression guard, not a no-op.
- Environment note: live re-execution of `dotnet test --filter "FullyQualifiedName~ModuleBoundariesTests"` and `dotnet build Anela.Heblo.sln` did not complete within this review session — the sandbox was under heavy build/CPU load (multiple concurrent `dotnet`/MSBuild processes from prior session activity) and repeatedly took 10+ minutes without finishing, consistent with the task prompt's warning about possible environment slowness. This is a resourcing issue, not a code issue: the prior task's review (`swap-complete-delivered-orders-job-to-new-contract.r1.md`) already ran `dotnet build`/`dotnet test` successfully against this same code earlier in the pipeline (0 build errors, 9/9 job tests passing), and this task's diff is purely additive test-data (no source/behavior change), so there is no plausible mechanism by which it could break the build. The implementation summary's reported results (30/30 `ModuleBoundariesTests` passing, clean `dotnet format`, full-suite pass count exactly one higher than the pre-task baseline) are fully consistent with the exact-match static diff verification performed here.
- The one crash observed during a build attempt (`Anela.Heblo.AccessMatrixGen` throwing `JsonException` during its post-build "Generating access matrix artifacts..." step) is in an unrelated tool (`backend/tools/Anela.Heblo.AccessMatrixGen/Program.cs`) with no connection to `ShoptetOrders`/`ShipmentLabels`/`ModuleBoundariesTests`, matching the implementation summary's description of it as a "known unrelated ... post-build warning."
