## Module
Leaflet

## Finding
`LeafletDocumentSummary` is defined in `backend/src/Anela.Heblo.Application/Features/Leaflet/UseCases/GetLeafletDocuments/GetLeafletDocumentsRequest.cs` (lines 26–35), inside the `GetLeafletDocuments` use case. However, it is also consumed by the `UploadLeaflet` use case:
- `UploadLeafletResponse.cs` imports `using Anela.Heblo.Application.Features.Leaflet.UseCases.GetLeafletDocuments;` to use `LeafletDocumentSummary` as the `Document` property type.
- `UploadLeafletHandler.cs` returns a `LeafletDocumentSummary` from its private `MapToSummary` method.

This creates a direct cross-use-case dependency: `UploadLeaflet` imports from `GetLeafletDocuments`.

## Why it matters
The filesystem.md spec states: `Contracts/ — Shared DTOs across use cases`. A DTO shared by more than one use case must live in the module's shared `Contracts/` folder, not inside any single use case folder. Having it in `GetLeafletDocuments/` implies ownership by that use case and forces `UploadLeaflet` to import from a sibling use case — the exact coupling the `Contracts/` pattern is meant to prevent.

## Suggested fix
1. Move `LeafletDocumentSummary` to a new file: `backend/src/Anela.Heblo.Application/Features/Leaflet/Contracts/LeafletDocumentSummary.cs` (the `Contracts/` folder already exists).
2. Update `GetLeafletDocumentsRequest.cs` and `UploadLeafletResponse.cs` / `UploadLeafletHandler.cs` to import from the new namespace.

No behaviour changes required.

---
_Filed by daily arch-review routine on 2026-09-07._

---
_Generated by [Claude Code](https://claude.ai/code)_
