### Question 1
FR-4 proposes two options for the frontend type rename: (a) propagate the new generated name `DailyInvoiceCountDto` through `useInvoiceImportStatistics.ts` and `InvoiceImportChart.tsx`, or (b) keep a local `DailyInvoiceCount` alias in the hook file so only one file changes. Either is acceptable and low-risk; the implementer should pick whichever keeps the diff smaller and note the choice in the PR. Recommendation: option (a), for consistency with the rest of the generated-client usage pattern in this codebase (no other hook appears to alias a generated type).

**Answer:** Go with option (a): rename the type to `DailyInvoiceCountDto` throughout `useInvoiceImportStatistics.ts` (import, re-export) and `InvoiceImportChart.tsx` (prop type, `.map()` usage stays the same since it's field access, not the type name). Do not introduce a local alias.

**Rationale:** A grep across `frontend/src/api/hooks/*.ts` shows every other hook (`useDashboard.ts`, `useIssuedInvoices.ts`, `useArticles.ts`, `useBankStatements.ts`, `useMaterialContainers.ts`, `useInvoiceClassification.ts`, `useRecurringJobs.ts`) imports and re-exports generated-client type names verbatim with zero aliasing precedent, so introducing an alias here would be a one-off inconsistency. The blast radius is also trivially small — only two files reference the type name (`useInvoiceImportStatistics.ts` and `InvoiceImportChart.tsx`) — so the "smaller diff" motivation for option (b) doesn't materially apply.

### Question 2
`InvoiceImportStatisticsSourceAdapterTests.cs` was identified via grep as referencing `IsBelowThreshold` but was not opened during spec authoring; the implementer should read it directly before editing to confirm exactly what assertions need updating (see FR-3).

**Answer:** The file has exactly one reference to `IsBelowThreshold`: line 73, `result[0].IsBelowThreshold.Should().BeFalse();`, inside `GetDailyCountsAsync_InvoiceDateBranch_ReturnsCountsGroupedByDay`. Delete that single assertion line; no other changes to this file are needed. The three `IsBelowThreshold = false` initializers inside `InvoiceImportStatisticsSourceAdapter.cs` itself (not the test file) are handled separately under FR-2.

**Rationale:** `InvoiceImportStatisticsSourceAdapterTests.cs` was read in full — its `_adapter.GetDailyCountsAsync(...)` result is `IReadOnlyList<DailyInvoiceCount>` (the Domain type), and the only assertion touching the removed property is that one `BeFalse()` check, which was only ever verifying the adapter's known dummy value (per the `IInvoiceImportStatisticsSource` XML doc, "the consumer decides thresholds"). Since `IsBelowThreshold` is being removed from the Domain type entirely (FR-1) and the adapter's own dummy-value assignment is removed (FR-2), the assertion has nothing left to check and simply drops out; the surrounding `Date`/`Count` assertions in that test (and all other tests in the file) are untouched, so the file continues to verify gap-filling and grouping behavior exactly as before.
