name: 🎭 E2E Nightly Regression Tests

on:
  # Scheduled - Every night at 2:00 AM CET (1:00 AM UTC)
  schedule:
    - cron: '0 1 * * *'  # 1:00 AM UTC = 2:00 AM CET

  # Manual trigger with optional test pattern filter
  workflow_dispatch:
    inputs:
      test_pattern:
        description: 'Test pattern (e.g., "catalog" or "auth", leave empty for all E2E tests)'
        required: false
        default: ''
        type: string
      browser:
        description: 'Browser to run tests on'
        required: false
        default: 'chromium'
        type: choice
        options:
          - 'chromium'
          - 'firefox'
          - 'webkit'
          - 'all'

env:
  STAGING_URL: https://heblo.stg.anela.cz
  NODE_VERSION: '18'

# Required permissions for test reporter to create check runs
permissions:
  checks: write      # Allow creating check runs for test results
  contents: read     # Allow reading repository contents

jobs:
  # Validate staging health before running tests
  validate-staging-health:
    name: 🏥 Validate Staging Health
    runs-on: ubuntu-latest

    steps:
      - name: 📊 Check Liveness Endpoint
        id: liveness
        run: |
          echo "🔍 Checking liveness endpoint..."
          http_code=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.STAGING_URL }}/health/live")

          if [ "$http_code" = "200" ]; then
            echo "✅ Liveness check passed"
            echo "liveness_status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ Liveness check failed with status $http_code"
            echo "liveness_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: 📊 Check Readiness Endpoint
        id: readiness
        run: |
          echo "🔍 Checking readiness endpoint..."
          http_code=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.STAGING_URL }}/health/ready")

          if [ "$http_code" = "200" ]; then
            echo "✅ Readiness check passed"
            echo "readiness_status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ Readiness check failed with status $http_code"
            echo "readiness_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: 📊 Check General Health Endpoint
        id: health
        run: |
          echo "🔍 Checking general health endpoint..."
          http_code=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.STAGING_URL }}/health")

          if [ "$http_code" = "200" ]; then
            echo "✅ General health check passed"
            echo "health_status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ General health check failed with status $http_code"
            echo "health_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: 📝 Health Check Summary
        run: |
          echo "## 🏥 Staging Health Check Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Endpoint | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Liveness | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Readiness | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| General Health | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 **Staging URL:** ${{ env.STAGING_URL }}" >> $GITHUB_STEP_SUMMARY
          echo "🕐 **Validated at:** $(date)" >> $GITHUB_STEP_SUMMARY

  # Run E2E tests against staging with parallel module execution
  run-e2e-tests:
    name: 🎭 Run E2E Tests (${{ matrix.module }})
    runs-on: ubuntu-latest
    needs: validate-staging-health
    strategy:
      fail-fast: false
      matrix:
        module:
          - catalog
          - issued-invoices
          - stock-operations
          - transport
          - manufacturing
          - core

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install dependencies
        working-directory: ./frontend
        run: npm install --legacy-peer-deps

      - name: 🎭 Install Playwright browsers
        working-directory: ./frontend
        run: |
          # Always install chromium for modular execution
          npx playwright install --with-deps chromium

      - name: 📂 List E2E test files for module
        working-directory: ./frontend
        run: |
          echo "🔍 Test files in module '${{ matrix.module }}':"
          find test/e2e/${{ matrix.module }} -name "*.spec.ts" -type f | sort
          echo ""
          echo "📊 Test count: $(find test/e2e/${{ matrix.module }} -name "*.spec.ts" -type f | wc -l)"

      - name: 🔐 Validate Azure credentials
        working-directory: ./frontend
        run: |
          if [ -z "${{ secrets.E2E_CLIENT_ID }}" ] || [ -z "${{ secrets.E2E_CLIENT_SECRET }}" ]; then
            echo "❌ E2E Azure credentials are not configured!"
            echo "Required secrets: E2E_CLIENT_ID, E2E_CLIENT_SECRET"
            exit 1
          fi
          echo "✅ Azure credentials validated"

      - name: 🧪 Run E2E tests for module
        id: run-tests
        working-directory: ./frontend
        continue-on-error: true
        run: |
          # Run tests for specific module
          npx playwright test --project=${{ matrix.module }}
        env:
          PLAYWRIGHT_BASE_URL: ${{ env.STAGING_URL }}
          CI: true
          # Azure service principal credentials for E2E authentication
          E2E_CLIENT_ID: ${{ secrets.E2E_CLIENT_ID }}
          E2E_CLIENT_SECRET: ${{ secrets.E2E_CLIENT_SECRET }}
          AZURE_TENANT_ID: ${{ secrets.REACT_APP_AZURE_TENANT_ID }}

      - name: 📊 Publish Test Results to GitHub
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: '🎭 E2E Test Results - ${{ matrix.module }}'
          path: 'frontend/test-results/junit.xml'
          reporter: 'java-junit'
          fail-on-error: false
          fail-on-empty: false

      - name: 📊 Upload E2E test results and HTML report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-test-results-${{ matrix.module }}-${{ github.run_number }}
          path: |
            frontend/test-results/
            frontend/playwright-report/
          retention-days: 30

      - name: 📸 Upload E2E screenshots (failures only)
        uses: actions/upload-artifact@v4
        if: always() && steps.run-tests.outcome == 'failure'
        with:
          name: e2e-failure-screenshots-${{ matrix.module }}-${{ github.run_number }}
          path: frontend/test-results/
          retention-days: 30

      - name: 📝 Create E2E test summary
        if: always()
        run: |
          echo "## 🎭 E2E Test Results - ${{ matrix.module }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Parse JSON results (more reliable than XML parsing)
          if [ -f frontend/test-results/results.json ]; then
            # Install jq if not available (should be pre-installed on GitHub runners)
            if ! command -v jq &> /dev/null; then
              echo "⚠️ jq not found, falling back to basic parsing"
              # Fallback to JUnit XML parsing
              JSON_AVAILABLE=false
            else
              JSON_AVAILABLE=true
            fi

            if [ "$JSON_AVAILABLE" = true ]; then
              # Parse JSON results (handles nested suite structure: .suites[].suites[].specs[])
              TOTAL=$(jq '[ .suites[].suites[]?.specs[]? ] | length' frontend/test-results/results.json 2>/dev/null || echo "0")
              PASSED=$(jq '[ .suites[].suites[]?.specs[]? | select(.ok == true) ] | length' frontend/test-results/results.json 2>/dev/null || echo "0")
              FAILED=$(jq '[ .suites[].suites[]?.specs[]? | select(.ok == false) ] | length' frontend/test-results/results.json 2>/dev/null || echo "0")
              SKIPPED=$(jq '[ .suites[].suites[]?.specs[]? | select(.tests[0].results[0].status == "skipped") ] | length' frontend/test-results/results.json 2>/dev/null || echo "0")

              # Check if any tests actually ran
              if [ "$TOTAL" -eq 0 ]; then
                echo "⚠️ **No tests were executed!**" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY
                echo "This usually means:" >> $GITHUB_STEP_SUMMARY
                echo "- Test configuration validation failed" >> $GITHUB_STEP_SUMMARY
                echo "- No test files matched the pattern" >> $GITHUB_STEP_SUMMARY
                echo "- Playwright crashed before running tests" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY
                echo "**Check the 'Run E2E Tests' step logs for details.**" >> $GITHUB_STEP_SUMMARY
              elif [ "$FAILED" -eq 0 ]; then
                echo "✅ **All tests passed successfully!**" >> $GITHUB_STEP_SUMMARY
              else
                echo "❌ **$FAILED test(s) failed.**" >> $GITHUB_STEP_SUMMARY
              fi

              echo "" >> $GITHUB_STEP_SUMMARY
              echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
              echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
              echo "| **Total Tests** | $TOTAL |" >> $GITHUB_STEP_SUMMARY
              echo "| ✅ **Passed** | $PASSED |" >> $GITHUB_STEP_SUMMARY
              echo "| ❌ **Failed** | $FAILED |" >> $GITHUB_STEP_SUMMARY
              if [ "$SKIPPED" -gt 0 ]; then
                echo "| ⏭️ **Skipped** | $SKIPPED |" >> $GITHUB_STEP_SUMMARY
              fi
              echo "" >> $GITHUB_STEP_SUMMARY

              # List failed tests with error details
              if [ "$FAILED" -gt 0 ]; then
                echo "### ❌ Failed Tests" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY

                # Extract failed test details from JSON (handles nested structure)
                jq -r '[ .suites[].suites[]?.specs[]? | select(.ok == false) ] | .[] | "**" + .title + "**\n- File: `" + .file + "`\n- Error: " + (.tests[0].results[0].error.message // "No error message") + "\n"' frontend/test-results/results.json >> $GITHUB_STEP_SUMMARY

                echo "" >> $GITHUB_STEP_SUMMARY
              fi

              # List skipped tests
              if [ "$SKIPPED" -gt 0 ]; then
                echo "### ⏭️ Skipped Tests" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY

                # Extract skipped test names from JSON (handles nested structure)
                jq -r '[ .suites[].suites[]?.specs[]? | select(.tests[0].results[0].status == "skipped") ] | .[] | "- " + .title' frontend/test-results/results.json >> $GITHUB_STEP_SUMMARY

                echo "" >> $GITHUB_STEP_SUMMARY
              fi
            fi
          fi

          # Fallback to JUnit XML if JSON is not available
          if [ ! -f frontend/test-results/results.json ] && [ -f frontend/test-results/junit.xml ]; then
            echo "⚠️ Using JUnit XML fallback (JSON results not found)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # Extract test counts from JUnit XML
            TESTS=$(grep -o 'tests="[0-9]*"' frontend/test-results/junit.xml | head -1 | grep -o '[0-9]*')
            FAILURES=$(grep -o 'failures="[0-9]*"' frontend/test-results/junit.xml | head -1 | grep -o '[0-9]*')
            ERRORS=$(grep -o 'errors="[0-9]*"' frontend/test-results/junit.xml | head -1 | grep -o '[0-9]*')
            SKIPPED=$(grep -o 'skipped="[0-9]*"' frontend/test-results/junit.xml | head -1 | grep -o '[0-9]*')

            TOTAL=${TESTS:-0}
            FAILED=$((${FAILURES:-0} + ${ERRORS:-0}))
            SKIP=${SKIPPED:-0}
            PASSED=$((TOTAL - FAILED - SKIP))

            # Check if any tests actually ran
            if [ "$TOTAL" -eq 0 ]; then
              echo "⚠️ **No tests were executed!**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "This usually means:" >> $GITHUB_STEP_SUMMARY
              echo "- Test configuration validation failed" >> $GITHUB_STEP_SUMMARY
              echo "- No test files matched the pattern" >> $GITHUB_STEP_SUMMARY
              echo "- Playwright crashed before running tests" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "**Check the 'Run E2E Tests' step logs for details.**" >> $GITHUB_STEP_SUMMARY
            elif [ "$FAILED" -eq 0 ]; then
              echo "✅ **All tests passed successfully!**" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **$FAILED test(s) failed.**" >> $GITHUB_STEP_SUMMARY
            fi

            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| **Total Tests** | $TOTAL |" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ **Passed** | $PASSED |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ **Failed** | $FAILED |" >> $GITHUB_STEP_SUMMARY
            if [ "$SKIP" -gt 0 ]; then
              echo "| ⏭️ **Skipped** | $SKIP |" >> $GITHUB_STEP_SUMMARY
            fi
            echo "" >> $GITHUB_STEP_SUMMARY

            # List failed tests if any (improved XML parsing)
            if [ "$FAILED" -gt 0 ]; then
              echo "### ❌ Failed Tests" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY

              # Use xmllint or grep with better pattern
              if command -v xmllint &> /dev/null; then
                xmllint --xpath "//testcase[failure]/@name" frontend/test-results/junit.xml 2>/dev/null | sed 's/name="//g' | sed 's/"$//' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY || echo "- Failed to parse test names with xmllint" >> $GITHUB_STEP_SUMMARY
              else
                # Fallback: Extract testcase names that have failure children
                grep -B 1 '<failure' frontend/test-results/junit.xml | grep '<testcase' | sed 's/.*name="\([^"]*\)".*/- \1/' >> $GITHUB_STEP_SUMMARY || echo "- Failed to parse test names from XML" >> $GITHUB_STEP_SUMMARY
              fi

              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # List skipped tests if any (improved XML parsing)
            if [ "$SKIP" -gt 0 ]; then
              echo "### ⏭️ Skipped Tests" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY

              # Use xmllint or grep with better pattern
              if command -v xmllint &> /dev/null; then
                xmllint --xpath "//testcase[skipped]/@name" frontend/test-results/junit.xml 2>/dev/null | sed 's/name="//g' | sed 's/"$//' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY || echo "- Failed to parse skipped test names with xmllint" >> $GITHUB_STEP_SUMMARY
              else
                # Fallback: Extract testcase names that have skipped children
                grep -B 1 '<skipped' frontend/test-results/junit.xml | grep '<testcase' | sed 's/.*name="\([^"]*\)".*/- \1/' >> $GITHUB_STEP_SUMMARY || echo "- Failed to parse skipped test names from XML" >> $GITHUB_STEP_SUMMARY
              fi

              echo "" >> $GITHUB_STEP_SUMMARY
            fi
          elif [ ! -f frontend/test-results/results.json ] && [ ! -f frontend/test-results/junit.xml ]; then
            echo "⚠️ **No test results found - tests may not have run**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Test Status: ${{ steps.run-tests.outcome }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "### 📋 Test Environment" >> $GITHUB_STEP_SUMMARY
          echo "- **URL:** ${{ env.STAGING_URL }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Browser:** ${{ github.event.inputs.browser || 'chromium' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Authentication:** Azure Service Principal" >> $GITHUB_STEP_SUMMARY
          echo "- **Executed at:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 View Detailed Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Test results are displayed above in the 'Annotations' section** - click on the test reporter check for detailed pass/fail status of each test." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "For HTML report with screenshots and traces:" >> $GITHUB_STEP_SUMMARY
          echo "1. **Download artifacts**: Go to Actions → This workflow run → Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "2. **Download** \`e2e-nightly-test-results-${{ github.run_number }}\` artifact" >> $GITHUB_STEP_SUMMARY
          echo "3. **Extract** the ZIP file" >> $GITHUB_STEP_SUMMARY
          echo "4. **Open** \`playwright-report/index.html\` in your browser" >> $GITHUB_STEP_SUMMARY

  # Aggregate results from all modules
  aggregate-results:
    name: 📊 Aggregate Test Results
    runs-on: ubuntu-latest
    needs: run-e2e-tests
    if: always()

    steps:
      - name: 📥 Download all test results
        uses: actions/download-artifact@v4
        with:
          pattern: e2e-test-results-*
          path: all-results

      - name: 📊 Aggregate and display results
        run: |
          echo "## 🎭 E2E Nightly Regression - Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Initialize counters
          TOTAL_TESTS=0
          TOTAL_PASSED=0
          TOTAL_FAILED=0
          TOTAL_SKIPPED=0

          # Aggregate results from all modules
          for dir in all-results/e2e-test-results-*/; do
            if [ -f "$dir/test-results/results.json" ]; then
              MODULE_TOTAL=$(jq '[ .suites[].suites[]?.specs[]? ] | length' "$dir/test-results/results.json" 2>/dev/null || echo "0")
              MODULE_PASSED=$(jq '[ .suites[].suites[]?.specs[]? | select(.ok == true) ] | length' "$dir/test-results/results.json" 2>/dev/null || echo "0")
              MODULE_FAILED=$(jq '[ .suites[].suites[]?.specs[]? | select(.ok == false) ] | length' "$dir/test-results/results.json" 2>/dev/null || echo "0")
              MODULE_SKIPPED=$(jq '[ .suites[].suites[]?.specs[]? | select(.tests[0].results[0].status == "skipped") ] | length' "$dir/test-results/results.json" 2>/dev/null || echo "0")

              TOTAL_TESTS=$((TOTAL_TESTS + MODULE_TOTAL))
              TOTAL_PASSED=$((TOTAL_PASSED + MODULE_PASSED))
              TOTAL_FAILED=$((TOTAL_FAILED + MODULE_FAILED))
              TOTAL_SKIPPED=$((TOTAL_SKIPPED + MODULE_SKIPPED))
            fi
          done

          # Display overall status
          if [ "$TOTAL_FAILED" -eq 0 ]; then
            echo "✅ **All tests passed successfully!**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **$TOTAL_FAILED test(s) failed.**" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          # Display aggregate metrics
          echo "### 📊 Overall Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Total Tests** | $TOTAL_TESTS |" >> $GITHUB_STEP_SUMMARY
          echo "| ✅ **Passed** | $TOTAL_PASSED |" >> $GITHUB_STEP_SUMMARY
          echo "| ❌ **Failed** | $TOTAL_FAILED |" >> $GITHUB_STEP_SUMMARY
          if [ "$TOTAL_SKIPPED" -gt 0 ]; then
            echo "| ⏭️ **Skipped** | $TOTAL_SKIPPED |" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### 📦 Module Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Detailed results for each module are available in their respective job summaries above." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # List modules
          echo "**Modules executed:**" >> $GITHUB_STEP_SUMMARY
          echo "- catalog" >> $GITHUB_STEP_SUMMARY
          echo "- issued-invoices" >> $GITHUB_STEP_SUMMARY
          echo "- stock-operations" >> $GITHUB_STEP_SUMMARY
          echo "- transport" >> $GITHUB_STEP_SUMMARY
          echo "- manufacturing" >> $GITHUB_STEP_SUMMARY
          echo "- core" >> $GITHUB_STEP_SUMMARY
